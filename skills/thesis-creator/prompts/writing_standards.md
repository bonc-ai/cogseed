# 论文写作规范

论文撰写的具体规范，包括标题层级、代码展示、图表占位符、学术写作风格。

---

## 标题生成规则(Markdown→Word 映射硬约束)

> **核心原则**：生成的 Markdown 标题必须能被 Pandoc / python-docx / markdown-it / Typora / VSCode Markdown 正确识别，映射为 Word Heading 1~4，支持自动目录、导航窗格、标题折叠和大纲视图。

### Markdown 标题与 Word Heading 映射

| Markdown 语法 | Word 映射 | 适用层级 | 示例 |
|--------------|-----------|---------|------|
| `# 标题名称` | Heading 1 | 章级标题 | `# 第一章 绪论` |
| `## 标题名称` | Heading 2 | 节级标题 | `## 1.1 研究背景` |
| `### 标题名称` | Heading 3 | 小节级标题 | `### 1.2.1 国外研究现状` |
| `#### 标题名称` | Heading 4 | 段级标题 | `#### 5.2.1 用户认证模块实现` |

### 格式硬约束

1. **标题符号后必须保留一个空格**
   - ✅ 正确：`## 研究背景`
   - ❌ 错误：`##研究背景`

2. **层级约束：禁止跨级跳跃**
   - ✅ 正确：`# → ## → ### → ####`
   - ❌ 错误：`# → ###`（跳过 `##`）

### 禁止行为(标题模拟禁令)

以下方式视为**格式错误**，禁止用于模拟标题层级：

| 禁止方式 | 错误示例 | 正确替代 |
|---------|---------|---------|
| 加粗文本 | `**第一章 绪论**` | `# 第一章 绪论` |
| 序号+普通段落 | `1.1 研究背景` | `## 1.1 研究背景` |
| 列表项 | `- 第一章 绪论` | `# 第一章 绪论` |
| 表格标题 | 表格内文字 | 对应级别 `#` 标题 |
| 单纯放大字体 | HTML `<font size>` | 对应级别 `#` 标题 |
| 中文符号模拟层级 | `===系统设计===` | `# 第三章 系统设计` |
| HTML 标签 | `<h1>标题</h1>` | `# 标题` |

### 论文结构标题示例

```markdown
# 摘要

# Abstract

# 第一章 绪论

## 1.1 研究背景

## 1.2 国内外研究现状

### 1.2.1 国外研究现状

### 1.2.2 国内研究现状

### 1.2.3 研究现状总结

# 第二章 关键技术

## 2.1 后端开发技术

# 第三章 需求分析

## 3.1 系统可行性分析

### 3.1.1 技术可行性

### 3.1.2 经济可行性

### 3.1.3 操作可行性

# 第四章 系统设计

## 4.1 系统架构设计

## 4.4 数据库设计

### 4.4.1 E-R 图设计

### 4.4.2 数据表结构设计

# 第五章 系统实现

## 5.1 XX 模块实现

#### 5.1.1 XX 功能实现

# 第六章 系统测试

## 6.1 测试目的

# 第七章 总结与展望

## 7.1 总结

## 7.2 展望

# 结论

# 参考文献

# 致谢
```

---

## 代码展示规则

> **核心原则**：论文是"论述"而非"代码仓库"，代码展示必须精炼且有设计说明。

### 规则清单

| 规则 | 说明 |
|------|------|
| 代码长度限制 | 每段代码**不超过 20 行**，超出的用 `// ...省略...` 代替 |
| 设计说明(前) | 每段代码前必须有**设计动机说明**(为什么这样做) |
| 效果分析(后) | 每段代码后必须有**效果分析**(达到了什么效果) |
| 禁止完整类展示 | ❌ 禁止直接贴完整 Service 类、Controller 类 |
| 禁止 CRUD 展示 | ❌ 通用的 CRUD 代码不展示，只描述即可 |
| 关键代码优先 | ✅ 只展示核心算法、关键逻辑、创新实现 |

### 正确示例

```markdown
#### 5.2.1 用户认证模块实现

系统采用 JWT 实现无状态认证，避免服务端存储 Session 的开销。

核心 Token 生成逻辑如下：

```java
public String generateToken(UserDetails userDetails) {
    Map<String, Object> claims = new HashMap<>();
    claims.put("username", userDetails.getUsername());
    // ...省略其他声明设置...
    return Jwts.builder()
        .setClaims(claims)
        .setExpiration(new Date(System.currentTimeMillis() + EXPIRATION))
        .signWith(SignatureAlgorithm.HS512, secretKey)
        .compact();
}
```

该方法通过 HS512 算法签名，设置了 24 小时的过期时间，既保证了安全性，
又避免了频繁登录的用户体验问题。
```

---

## 图表占位符规范

> **核心原则**：LLM 无法直接生成图片，必须使用**占位符机制**标记图表位置。

### 图表占位符格式

```markdown
<!-- 图表占位符：图4-1 系统架构图 -->
> 📊 **[图表占位符]**
> - **图表编号**：图4-1
> - **图表名称**：系统整体架构图
> - **图表类型**：架构图(建议使用 Draw.io / Visio 绘制)
> - **内容描述**：展示系统的分层架构...
> - **绘图要点**：
>   - 使用矩形表示各层
>   - 使用箭头表示调用关系
<!-- 图表占位符结束 -->
```

### 表格占位符格式

```markdown
<!-- 表格占位符：表4-1 用户表结构 -->
> 📋 **[表格占位符]**
> - **表格编号**：表4-1
> - **表格名称**：用户信息表(t_user)结构设计

| 字段名 | 类型 | 长度 | 允许空 | 主键 | 说明 |
|--------|------|------|--------|------|------|
| id | BIGINT | 20 | 否 | 是 | 用户ID，自增 |
| username | VARCHAR | 50 | 否 | | 用户名，唯一 |
<!-- 表格占位符结束 -->
```

---

## 学术写作风格

### AI 禁用词汇

- ❌ 「此外」「值得注意的是」「不可否认」「综上所述」
- ❌ 「首先...其次...最后...」(过度模板化)
- ❌ 「XXX是一种...，它具有...的特点」(教科书式)
- ✅ 「研究表明」「据统计分析」「笔者认为」「从实证角度来看」

### 必须包含的学术元素

| 元素 | 要求 | 示例 |
|------|------|------|
| 文献引用 | 关键论点必须有引用 | 「已有研究表明，RAG 技术能有效提升问答准确率[12]」 |
| 数据支撑 | 用数据论证观点 | 「系统响应时间从 500ms 优化至 80ms」 |
| 对比分析 | 技术选型需对比 | 「相较于 MySQL，PostgreSQL 在 JSON 查询方面具有优势」 |
| 方法论证 | 解释"为什么" | 「采用 Redis 缓存的主要原因是其高性能读写能力」 |

---

## 国内外研究现状模板

```markdown
### 1.2 国内外研究现状

#### 1.2.1 国外研究现状

在 XX 领域，国外学者较早开展了相关研究。Smith 等人[1]于 2018 年提出了
XX 方法，该方法的创新点在于...，但存在 XX 局限性。随后，Johnson 等人[2]
在 2020 年对该方法进行了改进，引入了 XX 技术...

#### 1.2.2 国内研究现状

国内学者在 XX 领域的研究起步相对较晚，但发展迅速。张三等人[4]于 2019 年
提出了...，该方法考虑了 XX 特点，更适合国内应用场景...

#### 1.2.3 研究现状总结

综上所述，国内外在 XX 领域已取得了一定成果，但仍存在以下问题：
1. XX 方面的研究不够深入...
2. XX 技术在实际应用中面临 XX 挑战...
```

---

## 可行性分析模板

```markdown
### 3.1 系统可行性分析

#### 3.1.1 技术可行性

本系统采用的技术方案基于成熟的开源框架和工具，具有较高的技术可行性。

**后端技术方面**，Spring Boot 框架经过多年发展，生态完善...

**前端技术方面**，React 框架具有组件化开发优势...

综上所述，本系统所采用的技术均为成熟可靠的主流技术，技术风险可控。

#### 3.1.2 经济可行性

本系统的开发和运维成本主要包括：
- **硬件成本**：系统可部署在云服务器上...
- **软件成本**：系统采用的技术栈均为开源软件...

#### 3.1.3 操作可行性

本系统的目标用户为 XX 人群，操作可行性分析如下：
- **用户接受度**：系统界面设计遵循主流的交互模式...
- **运维支持**：系统提供完善的日志记录和监控告警功能...
```