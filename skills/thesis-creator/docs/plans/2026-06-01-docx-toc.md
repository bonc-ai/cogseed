# DOCX 自动目录 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 让 `thesis-creator` 导出的 Word `.docx` 自动包含 Word 原生目录字段，并放在摘要/Abstract 后、第一章前。

**Architecture:** 在现有 `python-docx` 导出链路中增加一个小型 OOXML TOC 字段写入函数，不替换导出器、不引入新依赖。`convert_md_to_docx()` 继续按 Markdown 元素顺序写文档，只在遇到第一个正文 `h1` 前插入目录页。

**Tech Stack:** Python、python-docx、pytest、zipfile/XML 文本断言。

---

### Task 1: 添加 DOCX 自动目录回归测试

**Files:**
- Create: `test/test_document_exporter_toc.py`
- Test target: `scripts/document_exporter/docx_writer.py`

**Step 1: Write the failing test**

Create `test/test_document_exporter_toc.py`:

```python
from pathlib import Path
from zipfile import ZipFile

from document_exporter.docx_writer import convert_md_to_docx


def read_document_xml(docx_path: Path) -> str:
    with ZipFile(docx_path) as archive:
        return archive.read("word/document.xml").decode("utf-8")


def test_inserts_word_toc_after_abstract_before_first_chapter(tmp_path):
    markdown_path = tmp_path / "paper.md"
    output_path = tmp_path / "paper.docx"
    markdown_path.write_text(
        "# 摘要\n\n"
        "这里是中文摘要。\n\n"
        "# Abstract\n\n"
        "This is the English abstract.\n\n"
        "# 第一章 绪论\n\n"
        "## 1.1 研究背景\n\n"
        "这里是研究背景。\n",
        encoding="utf-8",
    )

    success, message = convert_md_to_docx(str(markdown_path), str(output_path))

    assert success, message
    document_xml = read_document_xml(output_path)
    assert 'TOC \\o "1-4" \\h \\z \\u' in document_xml
    assert document_xml.index("Abstract") < document_xml.index("TOC")
    assert document_xml.index("TOC") < document_xml.index("第一章 绪论")
```

**Step 2: Run test to verify it fails**

Run from `E:/AIAgent/thesis-creator/.claude/skills/thesis-creator`:

```bash
PYTHONPATH=scripts python -m pytest test/test_document_exporter_toc.py::test_inserts_word_toc_after_abstract_before_first_chapter -v
```

Expected: FAIL because `TOC \o "1-4" \h \z \u` is not present in `word/document.xml`.

---

### Task 2: 插入 Word 原生目录字段

**Files:**
- Modify: `scripts/document_exporter/docx_writer.py:1-20`
- Modify: `scripts/document_exporter/docx_writer.py:680-766`
- Test: `test/test_document_exporter_toc.py`

**Step 1: Add helper functions**

In `scripts/document_exporter/docx_writer.py`, add these helpers near `add_page_break()`:

```python
def add_table_of_contents(doc):
    para = doc.add_paragraph()
    para.alignment = WD_ALIGN_PARAGRAPH.CENTER
    para.paragraph_format.first_line_indent = Cm(0)

    title_run = para.add_run("目录")
    set_chinese_font(title_run, "黑体", 16, bold=True)
    title_run.font.color.rgb = RGBColor(0, 0, 0)

    toc_para = doc.add_paragraph()
    toc_para.paragraph_format.first_line_indent = Cm(0)

    fld_begin = OxmlElement("w:fldChar")
    fld_begin.set(qn("w:fldCharType"), "begin")

    instr_text = OxmlElement("w:instrText")
    instr_text.set(qn("xml:space"), "preserve")
    instr_text.text = 'TOC \\o "1-4" \\h \\z \\u'

    fld_separate = OxmlElement("w:fldChar")
    fld_separate.set(qn("w:fldCharType"), "separate")

    placeholder = OxmlElement("w:t")
    placeholder.text = "请在 Word 中右键更新目录"

    fld_end = OxmlElement("w:fldChar")
    fld_end.set(qn("w:fldCharType"), "end")

    for element in (fld_begin, instr_text, fld_separate, placeholder, fld_end):
        run = toc_para.add_run()
        run._r.append(element)

    add_page_break(doc)
```

**Step 2: Add title classification helpers**

Add near heading constants:

```python
ABSTRACT_H1_SECTIONS = {"摘要", "Abstract"}


def is_abstract_heading(text: str) -> bool:
    normalized = text.strip()
    return normalized in ABSTRACT_H1_SECTIONS


def should_insert_toc_before_heading(text: str, level: int, seen_abstract: bool, toc_inserted: bool) -> bool:
    if toc_inserted or level != 1:
        return False
    if is_abstract_heading(text):
        return False
    return seen_abstract
```

**Step 3: Wire TOC insertion into conversion loop**

In `convert_md_to_docx()`, before processing elements, initialize:

```python
seen_abstract = False
toc_inserted = False
```

Replace the heading branches with:

```python
if elem_type in {"h1", "h2", "h3", "h4"}:
    level = int(elem_type[1])
    heading_text = elem[1]
    if should_insert_toc_before_heading(heading_text, level, seen_abstract, toc_inserted):
        add_table_of_contents(doc)
        toc_inserted = True
    add_heading(doc, heading_text, level)
    if level == 1 and is_abstract_heading(heading_text):
        seen_abstract = True
```

Keep the non-heading branches unchanged.

**Step 4: Run the focused test**

```bash
PYTHONPATH=scripts python -m pytest test/test_document_exporter_toc.py::test_inserts_word_toc_after_abstract_before_first_chapter -v
```

Expected: PASS.

---

### Task 3: 覆盖没有 Abstract 但有摘要的情况

**Files:**
- Modify: `test/test_document_exporter_toc.py`
- Test target: `scripts/document_exporter/docx_writer.py`

**Step 1: Add another failing test if current logic does not cover it**

Append:

```python
def test_inserts_word_toc_after_chinese_abstract_when_no_english_abstract(tmp_path):
    markdown_path = tmp_path / "paper.md"
    output_path = tmp_path / "paper.docx"
    markdown_path.write_text(
        "# 摘要\n\n"
        "这里是中文摘要。\n\n"
        "# 第一章 绪论\n\n"
        "正文开始。\n",
        encoding="utf-8",
    )

    success, message = convert_md_to_docx(str(markdown_path), str(output_path))

    assert success, message
    document_xml = read_document_xml(output_path)
    assert document_xml.index("摘要") < document_xml.index("TOC")
    assert document_xml.index("TOC") < document_xml.index("第一章 绪论")
```

**Step 2: Run test**

```bash
PYTHONPATH=scripts python -m pytest test/test_document_exporter_toc.py -v
```

Expected: PASS if Task 2 logic is sufficient. If it fails, adjust `seen_abstract` handling only; do not broaden behavior beyond `h1` headings.

---

### Task 4: 更新模块上下文文档

**Files:**
- Modify: `.vibe-context/project/modules/document_exporter.yaml`

**Step 1: Update feature list**

Add this feature under `features`:

```yaml
  - id: "word-toc"
    name: "Word 自动目录"
    description: "导出 DOCX 时在摘要/Abstract 后、第一章前插入 Word 原生 TOC 字段"
    added_at: "2026-06-01"
    details:
      - "目录字段使用 TOC \\o \"1-4\" \\h \\z \\u"
      - "目录来源为 Word Heading 1-4"
      - "页码需用户在 Word/WPS 中更新域后生成"
```

**Step 2: Update responsibilities**

Add:

```yaml
  - "在 Word 文档中插入可更新的自动目录字段"
```

**Step 3: Update key functions**

Add:

```yaml
  - name: "add_table_of_contents"
    module: "scripts/document_exporter/docx_writer.py"
    purpose: "插入 Word 原生 TOC 字段，覆盖 Heading 1-4"
```

---

### Task 5: 跑完整相关测试并检查导出命令

**Files:**
- Test: `test/test_document_exporter_toc.py`
- Existing related tests if present: `test/test_document_exporter_preflight.py`, `test/test_document_exporter_table.py`

**Step 1: Run TOC tests**

```bash
PYTHONPATH=scripts python -m pytest test/test_document_exporter_toc.py -v
```

Expected: all PASS.

**Step 2: Run all document exporter tests that exist**

```bash
PYTHONPATH=scripts python -m pytest test/test_document_exporter_toc.py test/test_document_exporter_preflight.py test/test_document_exporter_table.py -v
```

Expected: PASS for existing files. If some listed files do not exist, run only the files present under `test/`.

**Step 3: Smoke-test CLI on a temporary Markdown file**

Create a temporary Markdown sample outside the repo or under a temporary directory, then run:

```bash
PYTHONPATH=scripts python -m document_exporter --input <temp>/paper.md --output <temp>/out --format docx
```

Expected: command succeeds and generated `.docx` contains `TOC \o "1-4" \h \z \u` in `word/document.xml`.

---

### Task 6: Commit changes only if requested

**Files:**
- `scripts/document_exporter/docx_writer.py`
- `test/test_document_exporter_toc.py`
- `.vibe-context/project/modules/document_exporter.yaml`
- `docs/plans/2026-06-01-docx-toc-design.md`
- `docs/plans/2026-06-01-docx-toc.md`

**Step 1: Check status**

```bash
git status --short
```

**Step 2: Commit only after explicit user approval**

If the user asks to commit, use a new commit, not amend.
