# Collaboration scope for this task

The requester explicitly picked the participants for this task. Treat that pick as
binding input, not as a hint.

Rules:

1. Every member listed under **Required executors** must end up with either a
   contribution of its own or an explicit blocking reason in your final delivery.
   Never silently drop one of them.
2. The list under **Available collaborators** is the working range for this task.
   It does not mean every member must run; decide per member from the task itself.
3. The order members are named in the request is the order they must work in when the
   task has any sequential intent. Declare dependent steps instead of dispatching the
   same work to several executors in parallel, and never reorder the requester's order.
4. Executors you cannot see or reach are not silently skipped: state the blockage in
   the delivery.
5. A successful call is not a finished task. The delivery must summarize what each
   participating executor contributed and which parts remain unfinished.

## Runtime injection

Task scope for this turn:

{{scope_lines}}
