interface:
  display_name: "验收与Evaluation设计"
  short_description: "设计功能验收、Agent能力评测与信任安全发布Gate"
  default_prompt: "使用 $acceptance-evaluation：为这个功能设计验收场景、Agent Evaluation与发布Gate。"

# SKILL-GATE:BEGIN
policy:
  allow_implicit_invocation: true
runtime:
  executor: "codex_skill_interpreter"
  binding_status: "target_state_not_run"
  skill_entrypoint: "../SKILL.md"
  schema_ref: "../schemas.json"
  permissions:
    direct_resource_access: false
    external_actions: "deny_without_explicit_approval"
    formal_asset_write: false
    candidate_write_only: true
  governance:
    level: "L5"
    risk_route: "Full"
    promotion_ceiling: "staged"
    production_release_allowed: false
    independent_review: "pending"
# SKILL-GATE:END
