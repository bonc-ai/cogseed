# Replay dataset — customer-profile-presales（stub，作者待填）

回放集：历史（脱敏）客户画像 → 期望作战方案的对照集，用于回归"改了映射后老案例不劣化"。

- 每条：`{profile（脱敏）, expected_session_plan_keys, tags[行业/层级/关系档]}`。
- 覆盖矩阵目标：行业(能源/煤炭/电网/制造) × 层级(P1/P2/P3) × 关系档(A/B/C) 的代表格。
- 只含脱敏/合成画像，**禁**真实客户轨迹/机密（见 §8.2 分发排除清单）。

> 登记最低档 ≥10 条评测（见 eval-cases.yaml）；发布评估档需 ≥20 条 + 完整学习记录（需引擎，不在本脚手架）。
