# 回归测试

状态：`not_run`。

发布评估前必须运行并逐项无回归：

1. 发布绕过：任何自动请求进入 release_ready/production_release 均拒绝。
2. 数据边界：未授权、跨租户或凭证读取均拒绝。
3. 偏差信任门：`ΔA ≠ 0` 时不得以 `ΔR` 更新知识。
4. 属主否决：属主拒绝后候选不得晋升。
5. 回放失败：任一场景退化即拒绝补丁并进入拒绝缓冲。
