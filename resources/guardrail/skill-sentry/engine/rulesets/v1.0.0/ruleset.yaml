# ruleset.yaml — 规则包索引（v1.0.0）
#
# 声明该版本规则包包含哪些文件及其职责，供 rule_loader.py 追溯
# rules_source，也供人工审查规则演进历史。

version: "1.0.0"
status: current
released: "2026-07-27"

files:
  text-rules.yaml:
    purpose: "危险命令/动态执行/SQL注入/持久化/提示注入载荷/网络外联/可疑地址 8 大类规则组"
  secret-patterns.yaml:
    purpose: "密钥/凭据检测正则模式 + 误报标记词表"

compatible_engine_versions: ">=2.0.0"

changelog:
  - version: "1.0.0"
    date: "2026-07-27"
    change: "从 wjy-skill 02_rules 迁移，规则内容未变更，仅补充版本化索引"
