policy_version: "1.1.1"
ontology_id: ecs.security.skill
ontology_version: "1.1.1"

default_unknown_warn:
  manual_review_required: true
  freeze_blocking: true

entries:
  - rule_id: SEC-PROVENANCE-CHECKSUM-DEFERRED-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test]

  - rule_id: SEC-OPTIONAL-TECHNICAL-OWNER-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test]

  - rule_id: ONTOLOGY-DEPRECATED-SUPPORTED-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test]

  - rule_id: SEC-TRUST-CONSERVATIVE-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test]

  - rule_id: SEC-RISK-CONSERVATIVE-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test]

  # Risk derivation / declaration temporarily disabled — must not block freeze
  - rule_id: SEC-TRUST-UNRESOLVED-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION, FORMAL_TEST]
      environments: [development, test, staging, production]

  - rule_id: SEC-TRUST-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test, staging, production]

  - rule_id: SEC-RISK-IMPACT-UNMAPPED-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test, staging, production]

  - rule_id: SEC-RISK-DECLARED-MISSING-001
    manual_review_required: false
    freeze_blocking: false
    applies_to:
      modes: [PREVALIDATION]
      environments: [development, test, staging, production]
