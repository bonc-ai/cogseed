# Compatibility manifest 模板（根目录 manifest.yaml）
# 由 artifact.yaml 单向生成；关键字段与安全引用必须与 Artifact Manifest 一致

id: REQUIRED_INPUT
name: REQUIRED_INPUT
version: REQUIRED_INPUT
description: REQUIRED_INPUT
owner: REQUIRED_INPUT
security_ontology: ecs.security.skill@1.1.1
security_manifest: references/security-manifest.yaml
entrypoint: SKILL.md
evals: evals/evals.json
