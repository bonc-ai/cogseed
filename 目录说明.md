# CogSeed 项目目录说明

> Canonical 仓库：`team-02/cogseed`
>
> CogSeed 是本地优先的多智能体协作工作台，也是跨 Agent 的个人能力资产层。

## 目录总览

```text
cogseed/
├── README.md               # Canonical 项目说明
├── README.zh-CN.md         # 中文同步说明
├── AGENTS.md               # 工程边界、数据和协作规则
├── CLAUDE.md               # 兼容的协作说明入口
├── docs/                   # 设计规格、实施计划和文档索引
├── src/                    # 应用源码
├── test/                   # 自动化测试
├── resources/              # 平台内容、静态资源和运行时清单
├── scripts/                # 构建、验证、迁移和启动辅助脚本
├── bin/                    # CLI 入口、worker 和运行时门禁
├── skills/                 # 项目级技能说明
├── vendor/                 # 仓库内置第三方资源
├── bootstrap.cjs           # Electron pre-tsx 启动引导
├── run.sh / run.cmd        # CogSeed source launcher
├── package.json            # npm 脚本、依赖和打包元数据
└── tsconfig.json           # TypeScript 配置
```

## 核心源码

### `src/main/`

Electron 主进程和 Node 后端：

- `ipc/`：参数验证和 feature 调用，不承载业务逻辑。
- `features/`：会话、项目、Connector、Messaging、Recall、KSTAR、CogSeed Runtime 等业务工作流。
- `model/`：模型适配、Core Agent 工具装配和授权路由。
- `storage.ts`、`paths.ts`：数据和路径 choke points。
- `preload.js`：向 renderer 暴露 `window.cogseed.{invoke, stream}`。

### `src/renderer/`

原生 HTML/CSS/JavaScript UI，不使用 TypeScript、JSX 或 renderer bundler。页面通过 IPC 与主进程通信。

### `src/core-agent/`

Core Agent 的会话、provider、工具定义、执行循环和持久化逻辑。

## 关键 feature

- `src/main/features/group_chat/`：Commander 与多 Agent 调度。
- `src/main/features/cogseed_runtime/`：隔离 Runtime worker 与 JSONL 协议。
- `src/main/features/cogseed_backend/`：任务、能力和协作后端。
- `src/main/features/recall/`：Cognition/Recall 捕获、资产、投影和证明。
- `src/main/features/p3394/`：Wake、Experience 和 KSTAR 边界。
- `src/main/features/local_agents/`：Claude Code、Codex、OpenCode、OpenClaw、Hermes。
- `src/main/features/connectors/`：OAuth 和 MCP Connector。

## 测试

- `test/main/`：主进程、feature、IPC、模型和跨层契约。
- `test/renderer/`：renderer 纯函数、页面 wiring 和 UI 状态。
- `test/scripts/`：构建、打包和审计脚本。
- `resources/test/`：平台内容的 Python 测试。

运行：

```bash
npm run typecheck
npm test
```

## 数据目录

Canonical source runtime 数据根：

```text
~/.cogseed/runtime-variants/cogseed/
```

用户数据位于 `data/<uid>/{cloud,local}/`。旧 `.orkas` 数据只通过迁移兼容层读取和复制，不再作为新数据写入目标。

## 推荐阅读顺序

1. `README.md`
2. `AGENTS.md`
3. `docs/README.md`
4. `docs/superpowers/specs/2026-08-11-cogseed-official-cutover-design.md`
