# Mate Agent 项目目录说明

> 适用位置：`~/Documents/Mate Agent/`
>
> 说明：这是第三版 Mate Agent 的本地可读副本，用于查看文稿、理解目录职责和后续整理。目录名保持英文，是为了和源码、脚本、测试引用一致，不影响运行。

---

## 一、项目定位

这是一个基于 Orkas 基座开发的 Mate Agent / Mate 智伴 第三版项目副本，当前包含：

- 源码
- 文稿
- 设计规格
- 实施计划
- 资源文件
- 测试
- 构建与启动脚本

---

## 二、目录总览

```text
Mate Agent/
├── README.md               # 英文项目介绍
├── README.zh-CN.md         # 中文项目介绍
├── CLAUDE.md               # 协作约束与开发规则
├── docs/                   # 设计文稿、规格、实施计划
├── src/                    # 核心源码
├── test/                   # 测试代码
├── resources/              # 应用资源与图片
├── scripts/                # 构建、校验、打包、辅助脚本
├── bin/                    # 命令行入口与运行时门禁脚本
├── skills/                 # 项目相关技能或能力说明
├── vendor/                 # 第三方内置资源
├── bootstrap.cjs           # 启动引导脚本
├── run.sh                  # macOS / Linux 启动脚本
├── run.cmd                 # Windows 启动脚本
├── package.json            # 依赖、脚本、工作区配置
└── tsconfig.json           # TypeScript 配置
```

---

## 三、各文件夹职责

### 1. `src/` — 源码区

项目的核心实现都在这里。

- `src/main/`：Electron 主进程、IPC、模型、权限、会话、P3394 逻辑。
- `src/renderer/`：桌面端前端界面、设置页、会话视图、中文/英文文案。
- `src/core-agent/`：核心 Agent 运行时、Provider、工具链、模型调用逻辑。
- `src/resources/`：品牌资源、图标母版等。

适合查看：
- 产品是怎么跑起来的
- 指挥官怎么调度 Agent
- KSTAR / Wake / Experience 是怎么实现的

---

### 2. `test/` — 测试区

自动化测试都在这里。

- `test/main/`：主进程、模型、权限、P3394、IPC 的测试。
- `test/renderer/`：前端组件、设置页、交互逻辑测试。
- `test/helpers/`：测试辅助工具。
- `test/fixtures/`：测试样例与静态数据。

适合查看：
- 每个功能是否有测试覆盖
- 某个行为是否已经被回归测试锁住

---

### 3. `docs/` — 文稿区

第三版的总体说明与确定件都放在这里（2026-07-27 已收敛）。

- `docs/README.md`：**唯一总体文档**——定位、已交付能力总览、确定件要点内化（引擎迁移设计、KSTAR 一致性审计、Sprint 2 技术交接的要点已折进本页 §4.1–§4.3）。
- `docs/superpowers/reports/`：仅保留 1 份签字确定件（能力基线 Evidence）；其余过程稿与已内化的 3 份确定件已删除，可从 Git 历史检出。

适合查看：
- 第三版的定位、边界和目标
- 当前已交付 / 在建能力的总览
- 当前能力基线（Evidence）与 KSTAR / Meta Skill Engine 迁移依据

---

### 4. `resources/` — 资源区

放应用内静态资源。

- 图片、品牌图、关键视觉等。
- 运行时会使用的嵌入资源。

适合查看：
- 项目品牌图
- 资源位截图

---

### 5. `scripts/` — 脚本区

放构建、检查、打包、初始化和辅助脚本。

常见用途：
- 环境准备
- 依赖安装
- 构建辅助
- 资源生成
- 平台特定处理

适合查看：
- 项目怎么启动
- 项目怎么打包
- 某些平台行为怎么被修正

---

### 6. `bin/` — 命令入口区

这里是命令行入口和运行时门禁脚本。

常见用途：
- 启动时的检查
- 运行时门禁
- 资源 / 权限 / 打包入口
- 代理脚本

适合查看：
- 某个功能是否需要先过门禁
- 打包后入口怎么工作

---

### 7. `skills/` — 技能区

存放与项目相关的技能说明或协作能力扩展。

适合查看：
- 某些工作流怎么标准化
- 项目里有哪些可复用技能

---

### 8. `vendor/` — 内置第三方资源

存放项目内嵌的第三方依赖或资源。

适合查看：
- 某些外部能力的本地封装
- 被项目直接带入仓库的第三方内容

---

## 四、根目录文件职责

### `README.md`
英文版项目说明。

### `README.zh-CN.md`
中文版项目说明。

### `CLAUDE.md`
仓库级协作约束、设计边界与工作规则。

### `package.json`
工作区、依赖和脚本入口。

### `tsconfig.json`
TypeScript 编译配置。

### `run.sh` / `run.cmd`
一键启动脚本。

### `bootstrap.cjs`
启动引导层。

---

## 五、当前这版最重要的文稿入口

### 1. 总目录（唯一总体文档）

```text
~/Documents/Mate Agent/docs/README.md
```

### 2. 唯一签字确定件

```text
~/Documents/Mate Agent/docs/superpowers/reports/2026-07-27-weekend-increment-capability-baseline-evidence.md
```

引擎迁移设计、KSTAR 一致性审计、Sprint 2 技术交接三份确定件的要点已内化进 `docs/README.md` §4.1–§4.3，原文可从 Git 历史检出。

---

## 六、查看建议顺序

如果你想最快看懂第三版，建议按这个顺序：

1. `README.zh-CN.md`
2. `docs/README.md`（唯一总体文档，看定位、能力总览与确定件要点 §4.1–§4.3）
3. `目录说明.md`
4. `docs/superpowers/reports/2026-07-27-weekend-increment-capability-baseline-evidence.md`（当前能力基线，唯一签字件）

---


---

## 三点新手阅读顺序

如果你第一次看这个目录，建议按下面顺序：

1. `README.zh-CN.md` — 先看项目中文总说明
2. `docs/README.md` — 再看文稿总目录
3. `目录说明.md` — 最后看目录职责说明

这样可以先理解“这是个什么项目”，再看“文稿在哪里”，最后看“每个文件夹是干什么的”。

## 七、为什么不把文件夹改成中文名

不建议把源码目录直接改成中文名，因为：

- 脚本和测试引用会失效
- 终端命令和路径维护成本会增加
- 和上游仓库结构不一致，不利于后续同步

所以更好的做法是：

- **目录名保持英文**
- **说明文档全部中文化**
- **入口文件集中索引**
