---
name: <skill-name>
description: >-
  <One-line: what capability this Skill provides + when to use it. Be specific about the
  business trigger. E.g. "Handle customer refund-dispute remediation: assess the dispute,
  decide the remedy, and draft the response. Use when a customer disputes a refund amount
  or escalates a billing complaint.">
---

# <Skill Title>

<1–2 sentences: the business capability. What problem it solves, for whom.>

## Trigger semantics
- **use_when**: <the concrete situation that should fire this skill>
- **do_not_use_when**: <the near-miss situation that must NOT fire it>
- **positive_examples**: ["<real user phrasing 1>", "<phrasing 2>"]
- **negative_examples**: ["<near-miss 1>", "<near-miss 2>"]

## Business context (ontology slice)
- **TBox** (concepts): <Entity{field_a, field_b}, …>
- **RBox** (rules): <R1: field_a ≤ 300; R2: high-complaint → soothe before explain>
- **ABox** (instances): <usually empty at authoring time>

## Workflow
`start → assess → decide → preview → confirm → execute → close`
(HITL gate at `confirm` for any write/execute action.)

## Input / output
See `references/input-contract.md` (three-layer input with owner_context) and
`references/output-contract.md` (actions/result/trace/audit_refs).

## Governance & non-claims
- `promotion_ceiling: staged` · `production_release_allowed: false`.
- Exposes contract field-positions only; resource/identity values injected by the Agent
  layer (`binding_resolved_by: agent_layer`). Never accesses real resources or holds tokens.
- Symbolic decides right/wrong; neural only proposes DRAFT wording.
