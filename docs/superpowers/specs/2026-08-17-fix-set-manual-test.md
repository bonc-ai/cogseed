# 手动测试：2026-08-17 修复集验证场景

- 日期：2026-08-17
- 构建：`d06baec2`（边界单边界可用 + 沉淀单路径收口 + 回执并集覆盖 + 语言硬闸）
- 目的：手动验证四个修复点，全部通过即本次修复集闭环
- 前置：实机已重启（跑 `d06baec2`）；测试数据为真实会话，测完可留可删

---

## 场景 A：中文任务 → 沉淀 rule 资产（B1 边界修复）

**操作**（新会话）：

```
帮我写一份 昆明城市 的资料，500 字
```

等 Commander 完成回复后，**再发一条新任务**触发闭环沉淀：

```
这个完成了，帮我写一份 贵阳城市 的资料，500 字
```

**预期**：
1. 对话正常完成，无报错
2. 沉淀后「认知/资产」页出现一条新 rule 资产，标题类似「写城市资料时应先收集数据再成文」（或该任务实际 lesson）
3. 资产来源显示 **系统沉淀**（KStar 线），不是会话抽取

**后台确认**（可让我查）：
```
- 日志无 "recall candidate blocked at the promotion gate" + rule_boundary_required
- 候选池：1 条 confirmed（无 superseded 双写）
- 资产 maturity=seed，lifecycleStatus=system_precipitated_unverified
```

---

## 场景 B：任务闭环只沉淀一次（A1 单路径收口）

**操作**：接场景 A，继续发第三个任务触发贵阳任务的闭环：

```
贵阳的也完成了，再帮我写一份 拉萨城市 的资料，500 字
```

**预期**：
1. 贵阳任务的 lesson 只沉淀**一条**候选/资产
2. 「认知」页待确认列表不出现重复内容（无 drain 线带来的第二条）

**后台确认**（可让我查）：
```
- 候选池按 judgment 过滤恰好 1 条
- 无同文本的 pending_review 残留
```

---

## 场景 C：多次使用后资产升档（B4 回执并集）

**操作**：在**新会话**（或同一会话）再发几个**同类资料任务**，让沉淀的 rule 资产被后续任务召回注入：

```
帮我写一份 杭州城市 的资料，500 字
（等完成）帮我写一份 苏州城市 的资料，500 字
（等完成）帮我写一份 南京城市 的资料，500 字
```

**预期**：
1. 后续任务中，「引用资产」出现场景 A 沉淀的昆明 lesson（注入生效）
2. 任务完成沉淀闭环后，该资产成熟度从 seed → **已迁移验证（transfer_validated）**
3. 多回合多回执场景也升档（不再因"单回执未覆盖全部"卡住）

**后台确认**（可让我查）：
```
- transfer-proofs 记录 receiptId + receiptExecutionId 非空
- 资产 maturity=transfer_validated
```

---

## 场景 D：语言硬闸（中文任务不产英文经验）

**操作**：新会话，发一个**中文**任务但故意让内容偏英文语境（验证模型若产出英文 lesson 会被丢弃，不污染池子）：

```
用英文写一段关于 smart home 的说明
```

**预期**：
1. 任务正常完成
2. 沉淀后「认知」页**不出现**英文标题/英文判断的候选（如 "For well-known..."）
3. 若任务有可复用经验，中文产出正常沉淀

**后台确认**（可让我查）：
```
- 日志可能见 "kstar review lesson dropped for language mismatch"（说明拦截生效）
- 候选池无英文主导 judgment
```

---

## 结果记录表

| 场景 | 结果 | 备注 |
|---|---|---|
| A. 单边界 rule 沉淀 | ☐ 通过 / ☐ 失败 | |
| B. 单路径无重复 | ☐ 通过 / ☐ 失败 | |
| C. 回执并集升档 | ☐ 通过 / ☐ 失败 | |
| D. 语言硬闸 | ☐ 通过 / ☐ 失败 | |

失败时把「认知」页截图或后台日志片段给我，我来定位。
