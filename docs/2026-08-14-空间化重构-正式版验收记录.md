# CogSeed 空间化重构（删法 B）正式版验收记录

**日期**：2026-08-14
**验收对象**：develop @ `3deb759` + 本次修复提交（F1/F2/F3）
**验收数据**：真实数据根 `~/.cogseed/runtime-variants/cogseed/data`（用户授权直接用真实数据，不搞副本）
**保险**：验收前快照 `devdata-20260814-092730.tar`（426M）+ 原始 `~/.cogseed/data` 未动 + 今晨备份

---

## 一、验收方法

1. 真实数据摸底（只读）：会话归属、空间、项目、附件/产物全量清点。
2. 冷启动真实数据：重启 app → v5 迁移幂等 no-op（marker 未重写、数据指纹 md5 一致）→ CDP 全流程。
3. 功能验证（CDP 实测）：面板结构 / 已删函数 / chat-use 绑定 / 5 视图切换 / 空间中心 / 空间工作台三 tab / 真实会话读取 / 旧项目会话可见性与可读性。
4. 回归：typecheck / smoke / 迁移 fixture / 定向测试 / 全量 test:js（对比基线 72 failed / 19 文件）。

## 二、通过项

| # | 项 | 结果 |
|---|---|---|
| 1 | 冷启动（真实数据重启） | ✅ v5 迁移幂等 no-op，数据指纹一致，启动日志干净 |
| 2 | 面板结构 | ✅ `panel-project` 不存在；spaces/workspace/conversation 正常 |
| 3 | 已删全局函数 | ✅ 7 个全 undefined（`refreshChatUseChips` 为活函数非遗留） |
| 4 | chat-use 绑定 | ✅ 三输入框均绑定 "1" |
| 5 | 5 视图切换 | ✅ 全 ok，全程 0 console 错误 |
| 6 | 空间中心 | ✅ 「我的空间」7 个真实空间卡全渲染 |
| 7 | 新空间工作台 | ✅ 空间页三 tab（任务/产物/资产）+ 空态文案正确 |
| 8 | 真实会话读取 | ✅ 会话消息/执行计划/附件面板完整加载 |
| 9 | typecheck / smoke / 迁移 fixture | ✅ 全过（fixture 6/6） |
| 10 | 全量 test:js | ✅ 与基线一致（失败均为基线既有 + flaky 波动，无实质新增） |

## 三、验收发现（交接未覆盖）与修复

### F1｜spaces.js 空间详情残留「空间内项目」区块（可见死 UI）
- **现象**：打开任一真实空间详情可见「空间内项目」空态 + 「新建项目」按钮；点击必弹"新建项目失败"（调已删 `projects.list/create/bindSpace` IPC）。
- **修复**：删除 `_loadProjects`/`_renderDetailProjects`/新建按钮/`_createProjectInSpace`/`setView('project')` 及删除确认文案中的项目表述；详情页只保留资源/本体区块。
- **验证**：CDP 实测空间详情无 `spaces-proj` 区块/按钮（HTML -530 字符）。

### F2｜旧项目会话 UI 完全不可见（数据可见性回归）
- **现象**：5 条旧项目会话（@PRD一致性检查Agent/@项目计划Agent/@竞品研究Agent/nn/你好）在会话列表与任何空间都不可见。机制：`listStartupConversations` 项目分页依赖 `projects.list`（已失效），renderer `renderConversationList` 又按 `!c.project_id` 过滤。
- **修复**（F2-A，用户选定方案）：
  - 后端 `chats.ts`：`listStartupConversations` 与 `listOldUnprojectedConversationPage` 的过滤改为「仅 `project_id && space_id` 视为空间绑定归属；纯 `project_id` 孤儿按 unprojected 放行」。
  - 前端 `conversation.js`：`renderConversationList`/`_loadOldUnprojectedConversations` 同语义过滤。
  - 保留 `_readScopedRawConversations` 物理根隔离（避免重复 cid 污染，chats.test 隔离用例守护）。
- **验证**：CDP 实测列表 15 → 20 条，4 个独特项目会话标题可见，点击 @PRD一致性检查Agent 打开 8 条消息完整加载，0 报错。

### F3｜~14 处 `projects.*` IPC 调用 + 5 处 `setView('project')` 残留（静默失效）
- **清单**：spaces / personal-ontology / onboarding / conversation / auto / agents / md-view-edit / kb-picker / search / library-transfer / user-workspace（+ conversation-info 死守卫），以及 `project-chat-input`/`project-chat-recipient-chip` 等已删元素引用。
- **处理**：
  - 删除项目分支/死函数/死 UI（onboarding 导入绑定块、agents 项目作用域、auto 收件人校验、search 项目结果跳转、md-view-edit/kb-picker 项目文件分支、library-transfer 项目库选项）。
  - 会话列表过滤、user-workspace chip、conversation 引用转发统一为空间化语义。
- **验证**：`grep` 全 renderer 无 `projects.*` IPC、无 `setView('project')`、无 `project-chat-input`（oss.js 的 projects 为 OSS 开源项目展示，与废弃概念无关，保留）。

## 四、测试适配（随行为变更更新断言）

- `chats.test.ts`：index repair 3 用例改为空间绑定语义（space_id + project_id）+ 新增孤儿项目会话可见断言。
- `category-tabs.test.ts`：agent-picker 库测试改为全局 only（3 用例）。
- `conversation-cross-task-reference.test.ts`：新任务引用不再携带项目作用域（2 用例）。
- `lazy-features.test.ts`：recipient picker 断言改为「不再引用 projects.scope」（1 用例）。
- `library-transfer.test.ts`：项目库选项归一为全局（3 用例）。
- `onboarding-finish-project.test.ts`：导入会话保持未分组、无项目 IPC（1 用例）。

## 五、最终状态

- **typecheck** ✅ 通过
- **smoke** ✅ OK
- **迁移 fixture** ✅ 6/6
- **定向测试** ✅ 全绿（改动相关）
- **全量 test:js**：与基线一致——失败均为基线既有（recall×6 / expert_team / category-tabs 9 / conversation-* / builtin / codesign / p3394×3 / touchpoint / workspace-open / bus-integration / lazy-features / static p3394）+ flaky 波动（kstar-preview-trigger、messaging-owner-bind-integration，隔离重跑全过）；**无实质新增失败**
- **CDP 综合验收**：面板 / 已删函数 / chat-use / 5 视图 / 7 空间卡 / F1 无项目区块 / F2 20 条含项目会话可读 / 0 console 错误 —— 全绿
- **App**：重启后运行中（PID 见进程，端口 9222），数据根为真实数据开发副本

## 六、发布前备注

- 🔴 对外发布纪律仍有效：打包版将读 `~/.cogseed/data`（当前不存在）；发布前在**副本**上做最终演练，勿直接在生产根跑未经验收版本。
- 遗留（刻意保留）：`project_id/projectId` 数据兼容字段、`project-layout.ts`/`paths.ts`/迁移脚本 v4/v5、T5.3 style.css 清理（延后）、`_projectsCache` 等惰性守卫。
- 教训（已存记忆）：引用归零判定必须覆盖 **IPC channel 字符串**与 **view 名**，不止 `features/projects` 与数据字段。

## 七、追加：问题 A（已有会话绑定空间）与问题 B（空间指令/画像注入断链）

**日期**：2026-08-14 下午（develop @ `fb98f4c` 之后，32 提交未 push）
**验收数据**：真实数据根（52 活会话仅 3 条绑 `sp_8ee07a1e79f6`，用户空间 111/产品经理333 未动）

### A1｜`conversations.setSpace` IPC + 侧栏/任务行「移至空间」入口
- **后端**：`chats.ts::setConversationSpace(uid, cid, spaceId|null, projectIdHint?)` —— 只写 `conversation.space_id`（与 `conversations.create({spaceId})` 语义一致，不动已废弃 `project_id`；F2 侧栏可见性不变，绑定后仍在侧栏同时进空间任务列表）。`ipc/index.ts` 增 `conversations.setSpace`（spaceId 空 = 解绑；非空校验 `spaces.spaceExists`）。
- **前端**：`conversation.js` 侧栏会话操作菜单加「移至空间/移至其他空间」→ 空间选择弹层（`spaces.list` 数据源，点选即绑，已绑显示当前 + 移出空间）；`workspace.js` 空间任务行「更多」→ 移出空间（解绑并刷新三 tab）。
- **验证**：CDP 实测 bind→空间任务列表出现 / unbind→消失 / 非法 spaceId 拒绝（`{ok:false,error:'invalid spaceId'}`）/ 侧栏菜单文案（未绑「移至空间」、已绑「移至其他空间」）/ 任务行移出全链路；单测 `chats.test.ts` 新增 3 条（绑定+列表纳入 / 解绑清字段 / 非法拒绝）。

### B1｜根因：`spaceId` 未穿透到 core-agent runner，空间指令/画像/context-policy 块从未注入主对话路径
- **现象**：绑定会话实测 system prompt **不含** `## Space instructions` / `## 当前角色画像` / `## Space context policy`。
- **根因**：`group_chat/bus.ts` 每 turn 已解析 `turnSpaceId` 并传入 `streamChatWithModel`（ChatOptions），但 `core-agent/client.ts::streamChatWithModel` **从未解构/转发 `spaceId` 给 `buildRunner`** → 核心 runner 的 `params.spaceId` 恒空 → runner.ts 里三个注入块（993/1000/1030 行）与空间 memory 层级、`project-instructions` 工具全部死代码。空间作用域（agents/skills 过滤）走 `turnSpaceScope` 另路，一直有效，造成「作用域生效但指令/画像没生效」的割裂。
- **修复**（5 行穿透）：`model/client.ts` `ChatOptions` 增 `spaceId?`；`core-agent/client.ts` 解构 + `buildRunner` 转发 `spaceId` + `modelTurnContextForLog` 打点 `space_id`（观测）。
- **验证**：
  - 新单测 `space_system_prompt_inject.test.ts` 4 条：① bus 把 space_id 透传 ChatOptions；② 孤儿会话不透传（对照）；③ `buildRunner` 带 spaceId → `resolvedSystemPrompt` 含 context policy + 空间指令 + 角色画像（含模板字段值、不含 `@proj:` 来源标记）；④ 无 spaceId → 三块全无（对照）。
  - 真机：绑定会话发消息 → 日志 `model turn ready` 出现 `space_id: 'sp_2...'`，证明全链路在运行中生效（提示词内容由单测③覆盖，避免真调 LLM）。
  - 顺带确认 `role_profile_inject.test.ts`（6 条）基线通过；`formatSpaceInstructionsForSystemPrompt`/`formatRoleProfileForSystemPrompt` 函数级行为无回归。

### 回归
- typecheck ✅ / smoke ✅ / 定向（chats 53、spaces 43、role_profile 6、space_system_prompt_inject 4、IPC 套件）✅
- 全量 test:js：**21 failed / 648 passed / 75 failed / 7670 passed** —— 失败**文件清单与基线完全一致**（recall×6 / expert_team / category-tabs / conversation-sidebar+produced-chips+copy-merge / builtin / codesign / p3394 asset-events+skill-validation-run+static / touchpoint / workspace-open / bus-integration / lazy-features / flaky kstar-preview-trigger），**无新增失败文件**。
- 测试数据已清理（cdp 验证用的会话删、空间删、无 spacetask-* 草稿残留）。

## 八、追加：侧栏空间分组 + @ 选择器按空间能力过滤（用户需求）

**日期**：2026-08-14 傍晚（用户提出两个交互需求后实现）

### C1｜侧栏按空间分组
- **需求**：侧栏结构 = 顶部「最近任务」（无空间会话）+「空间」区（每个绑定会话的空间一个可折叠组，组内为该空间会话）。
- **实现**（`conversation.js`）：
  - `renderConversationList` 分组语义从 `!(project_id && space_id)` 改为 **`!space_id` = 最近任务 / 有 `space_id` = 空间组**（纯 project_id 旧孤儿仍归最近任务，F2-A 不回归）。
  - 新增 `_ensureSidebarSpaces`（`spaces.list` 懒加载，失败静默降级）、`_spaceConversationMap`、`_renderSpaceSidebarGroup`（组头=空间名+chevron+会话数，默认展开，手动折叠记忆于 `_sidebarSpaceCollapsed`）。
  - 折叠/展开事件绑定在 `_bindConversationSidebarItems`（`data-conv-space-toggle`）。
  - locale：`sidebar.recent_tasks`（最近任务）/ `sidebar.spaces_section`（空间）；CSS：`.conv-list-space-title` 分区标题 + 空间组内会话缩进。
- **验证**：CDP 实测「最近任务」「空间」两分区标题、空间组（按最近活跃排序）、折叠⇄展开切换正常；真实数据下 111 空间组显示其 2 条会话。

### C2｜@ 选择器按空间能力配置过滤（agents + skills）
- **需求**：绑定空间的会话，@ 只能 @ 该空间能力配置的智能体与技能（模板 bundle ∪ extra）。
- **实现**：
  - 后端新增 `spaces.scope.resolve` IPC（校验 spaceId 存在 → `spaces.resolveSpaceScope`，语义与 runner 执行作用域同源 S1：空配置/全失效 → null = 全局）。
  - `agents.js::_refreshAgentPickerProjectContext`：同步判定当前会话 `conv.space_id`；无空间 → 全局（原状）；有空间 → 异步解析作用域设置 `_pickerBoundAgentIds/_pickerBoundSkillIds`（agents/skills 两个 tab 的既有过滤逻辑直接生效）。`_pickerProjectContextLoading` 壳防旧列表可点。
- **验证**：CDP 实测——111 空间（extra 1 agent + 1 skill）绑定会话：agents tab 仅「指挥官+选品分析Agent」，skills tab 仅「本体抽取」；孤儿会话全局 43 agents 不受影响。

### 回归（C1/C2）
- typecheck ✅ / 定向（spaces 32、space_system_prompt_inject 4、agent-picker/sidebar renderer 7、conversation-sidebar 65 过 1 基线遗留）✅
- `conversation-sidebar.test.ts` 菜单数组断言随新增「移至空间」菜单项更新（功能适配，非回归）；遗留失败仅剩空间化重构未适配的项目分页用例（`projectsContainer` 已删）。
- 全量 test:js 复跑确认失败文件清单与基线一致（无新增失败文件）。
- 测试数据无新增残留（CDP 验证仅读真实会话/空间，未写；spacetask-* 草稿无）。

## 九、追加：侧栏三段结构定稿（置顶 → 空间 → 最近，v2 设计落地）

**日期**：2026-08-14 晚间（用户三轮设计确认后实现：置顶做 / 最近不要时间桶标题 / 会话行装饰图标删 / 进行中不显示 / ⋯ hover 显隐 / 移到空间只在 ⋯ 内）

### 侧栏结构（renderConversationList 重写）
```
置顶（pinned，无则整段隐藏，可折叠）
空间（每空间折叠组：空间名 + chevron + 会话数；分区可折叠；标题行 ＋ = 新建空间）
最近任务（无空间会话平铺，按最近活跃倒序，无时间桶标题；分区可折叠；标题行 ＋ = 新建任务）
```
- 折叠状态持久化 `chat.sidebar.collapse.v1`（localStorage：三分区 + 每空间组）。
- 新建任务 ＋：展开内联输入条（`conv-sidebar-new-task`），回车 `conversations.create({kind:'normal',title})` 建普通会话并打开，Escape 收起。
- 新建空间 ＋：`setView('workspace')` 触发 workspace.js 懒加载，轮询等 `window.openWorkspaceCreate`（workspace.js 新增暴露：renderWorkspace 后 `_openCreate(null)` 弹新建空间）。
- 会话行：删 agent 头像栈 meta 行、删进行中/排队徽标（`_updateConvSidebarBadge` 空化，仅保留全局运行 chip + 会话头刷新）；⋯ 原本就是 hover 显隐（CSS 无改动）。
- index.html 删顶部 `sidebar.conversations` 分区标题（三段标题在列表内渲染）。
- locale 新增 `sidebar.pinned_section` / `sidebar.new_task` / `sidebar.new_space` / `sidebar.new_task_ph`（四语）。
- pinned 会话只在置顶区出现（不进空间组/最近区，避免重复）。

### 验证（CDP 实测）
- 三段标题顺序、置顶区出现/折叠/无置顶自动隐藏、最近区无桶标题、行无 meta、空间组折叠持久化、最近 ＋ 展开输入条→回车真实创建会话、空间 ＋ → 懒加载后弹新建空间弹窗、取消可关 —— 全绿。
- 测试数据已清理（cdp-* 会话删、无 spacetask 草稿；用户手动建的「二千e q e」「教育 3」空间未动）。
- 回归：typecheck ✅；`conversation-sidebar.test.ts` 恢复基线（仅剩空间化项目分页遗留 1 例，stub container 缺 `querySelector` 已做安全调用适配）；全量 test:js 失败文件清单与基线一致。

## 十、追加：工作空间 chip 改为「空间选择器」+ 空间卡「删除空间」

**日期**：2026-08-14 深夜（用户两需求：chip 选空间 / 删除空间后任务落最近任务）

### D1｜聊天输入区「工作区: userWorkspace」→ 空间选择器（user-workspace.js 重写）
- **现状**：chip 原为旧「文件夹工作区」残留——点开选默认/最近/文件夹（workspace.getInfo/set/selectDirectory IPC），产品已换「空间」概念但此 chip 未跟上。
- **改动**：重写 user-workspace.js 为空间选择器——
  - chip 文案「工作空间: <空间名>」（未选 = 默认工作区）。
  - 下拉：搜索框（搜索空间名）+「默认工作区」项 + 空间列表（spaces.list，最近活跃排序，当前勾选）+「新建工作空间」（openWorkspaceCreate 弹窗）。
  - conversation 面板：选空间 → `conversations.setSpace(cid, sid)` 绑定/切换；选默认 → 解绑（回最近任务）。
  - new-chat 面板：选空间记忆到 localStorage `chat.newChatSpaceId.v1`，后续新对话创建带 space_id（handleNewChatSubmit 创建 body 加 spaceId，读 `window.getNewChatSpaceId`）。
  - 旧文件夹逻辑（workspace.getInfo/set/reset/selectDirectory）彻底移除。
- **bug 修复**：`{...旧, ...后端返回}` 合并时解绑后 space_id 键缺失导致旧值残留——两处（user-workspace `_pickSpace` + conversation `_openConversationSpacePicker`）改为显式 set/delete space_id。

### D2｜空间卡「⋯」→ 删除空间（workspace.js）
- 三个点菜单加「删除空间」（红色操作区，meta 之下）。
- 点击 → confirm 弹窗（列任务数：`删除空间「{name}」？空间内 {count} 个任务将移到「最近任务」…`）→ `spaces.delete` → 刷新空间列表 + 侧栏（renderConversationList）。
- 后端 `spaces.ts::deleteSpace` 增强：删除空间时**批量清空该空间所有会话 space_id**（listSpaceConversations → setConversationSpace(cid, null)），会话落到「最近任务」，不残留编号（否则空间没了、space_id 还在 = 会话消失）。
- 新增测试：spaces.test.ts「deleteSpace 清空该空间会话的 space_id」。

### 验证（CDP 实测）
- chip：new-chat 显示「工作空间：默认工作区」、111 会话显示「工作空间：111」；下拉搜索/列表/新建/勾选全正常；conversation 切换绑定、选默认解绑（chip 回「默认工作区」）；new-chat 选空间后 `getNewChatSpaceId` 返回正确。
- 删除空间：卡片 ⋯ → 确认文案含任务数 → 空间删除 → 两条会话 space_id 清空（落最近任务）。
- 数据已清理（临时空间/会话删净，无 spacetask 草稿；用户「二千e q e」「教育 3」未动）。
- 回归：spaces 29、user-workspace 2、space_system_prompt_inject 4 全过；typecheck ✅；全量 test:js 失败文件清单与基线一致。

## 十一、追加：全局 bug 排查与修复（D 系列改动链路）

**日期**：2026-08-15 凌晨（用户「找一下bug全局」后系统性排查侧栏/chip/删除空间/@ 过滤链路）

| # | Bug | 修复 |
|---|---|---|
| 1 | 删除空间后侧栏残留「已删空间组」：workspace.js 删除回调只调 renderConversationList，本地 `conversations` 数组会话 space_id 未清（后端已清） | workspace.js 删除回调：遍历 conversations 清该空间会话 space_id + `window.invalidateSidebarSpaces` |
| 2 | 新建空间后侧栏不显示该空间组：`_sidebarSpaces` 启动时缓存，新空间不在缓存，即使有会话也被过滤掉 | renderConversationList 空间组改为「会话里出现过的全部 space_id 并集」（缓存未刷新时用 sid 兜底名）；新增 `window.invalidateSidebarSpaces`（新建/删除空间后调用） |
| 3 | new-chat 面板 @ 选择器不按 chip 所选空间过滤：只有 chat-recipient-chip 解析会话空间，new-chat 无 cid 恒全局，与「创建后归该空间」不一致 | agents.js `_refreshAgentPickerProjectContext`：new-chat-recipient-chip 读 `window.getNewChatSpaceId()` → 有则解析空间作用域 |
| 4 | old_unprojected 桶语义不一致：后端 `listStartupConversations`/`listOldUnprojectedConversationPage` 用旧 `project_id && space_id`，把绑定空间会话算进「最近任务」懒加载计数（虚高）；且 startup 只返回项目分页 10 条，侧栏空间组需要全量 | 统一为 `!space_id`：空间会话不进 deferred 桶、始终返回（侧栏空间组全量展示）；前端 `_replaceConversationSlice` 同步；chats.test.ts 2 用例断言适配（语义有意变更） |
| 5 | chip 下拉异步竞态（点开后快速点别处菜单可能复活） | 窗口极小（spaces.list 快），未处理，标注 |
| 6 | new-chat 记忆的空间被删除后 chip 显示失效 sid | user-workspace `_loadSpaces` 成功加载后清理不在列表的 `_newChatSpaceId` |

**验证（CDP）**：新空间侧栏组显示（fallback 名 → invalidate 后真名）；删除空间后组消失 + conversations space_id 清空；new-chat 经 UI 选学生空间后 @ agents 仅 4 项（指挥官+3 bundle）、skills 5 项 bundle；chats 53 全过。数据已清理。

## 十二、追加：旧「工作空间」功能迁移（重命名/指令/能力/失效清理/主技能）+ 空间产物聚合修复

**日期**：2026-08-15 凌晨（用户确认做 12345 五项；本体 tab 不做；发现产物不落位 bug）

### E1｜产物不落位（bug）——空间「产物」tab 缺 AI 产出文件
- **现象**：用户在「二千e q e」空间产出的 `周末去北京怎么玩.docx` 不出现在空间产物 tab。
- **根因**：`spaces_artifacts.listSpaceArtifacts` 只聚合两类——附件（chat_attachments）+ web artifact（chat_artifacts）；AI 工具（write_file/markdown_to_pdf/office）产出的文件是**消息 produced[] 登记的工作区文件**（`userWorkSpace/<slug>/...`），从未被扫。且 `registerProducedOutputHooks` 无调用方（finalizeProducedFile 空操作，无落盘索引）。
- **修复**：`listSpaceArtifacts` 新增 `scanProducedFiles`——遍历空间会话消息 produced[]（getMessages 1000 条），验证文件存在后按文件名去重聚合（附件优先）。前端 `_artifactCategory` 按扩展名分类（.docx → 文档）。
- **测试**：新增 `spaces_artifacts.test.ts` 2 用例（聚合 word/pdf / 不存在的路径跳过）。CDP 实测「二千e q e」产物列表出现 `周末去北京怎么玩.docx`。

### E2｜旧「工作空间」5 项功能迁移（新空间中心补全）
侧栏旧入口「工作空间」（spaces.js，仍保留）持有 6 项新面板缺失能力；用户确认做前 5 项（本体 tab 不做）：
1. **重命名**：空间卡 ⋯ 菜单加「重命名」（prompt → `spaces.update{name}` → 刷新 + 侧栏缓存失效）。✓ CDP
2. **编辑指令**：设置抽屉默认目标/指令改 textarea + 保存（`spaces.instructions.set`，4000 上限；`_configInstructionsDraft` 草稿保留）。✓ CDP
3. **编辑能力**：设置抽屉 Task Agent / Skill 卡加「调整」→ 独立弹窗（模板 bundle 固定不可移除 + extra 勾选；保存 diff → `spaces.resources.add/remove`）；角色卡只读（新建时定）。✓ CDP
4. **失效清理**：设置抽屉显示 invalid_count + 「清理」→ `spaces.resources.pruneInvalid`。✓ CDP
5. **主技能**：设置抽屉「主技能」行 + 选择弹窗（skills.list 单选、可清除 → `spaces.update{main_skill_ref}`）；IPC `spaces.update` 透传 main_skill_ref（用 hasOwnProperty 判断显式传参）。✓ CDP
- CSS：`.ws-more-action` / `.ws-config-actions` / `.ws-config-row` / `.ws-config-value` / textarea 样式。

### 回归（E1/E2）
- typecheck ✅；spaces_artifacts 2 过；spaces/chats 全过。
- 全量 test:js 失败文件清单与基线一致（无新增失败文件）。
- 测试数据已清理（cdp-* 空间/会话删净）。备注：「教育 3」空间（用户手动建）在排查期间消失，非本会话脚本删除（严格只删 cdp- 前缀/自建 sid），疑为用户自行删除。

## 十三、追加：空间产物「确认」流程 + 「打开」接真（方案 A，用户拍板）

**日期**：2026-08-15（用户选方案 A：AI 产出走候选→确认；附件/网页直接正式；打开用系统默认软件）

### F1｜产物确认流程（AI 产出候选 → 用户确认 → 正式）
- **后端** `spaces_artifacts.ts`：
  - `SpaceArtifactEntry` 加 `source`（attachment/artifact/produced）+ `confirmed` + `path`（绝对路径，供打开）。
  - 确认清单落盘：`<spaceContentDir>/confirmed_artifacts.json` = `{ [cid]: string[] }`（随 deleteSpace 一并删）。
  - `scanProducedFiles`：AI 产出默认候选（confirmed = 清单含）；附件/网页直接 `confirmed: true`。
  - 新增 `confirmSpaceArtifact(uid, sid, cid, name)`（幂等写入清单）。
- **IPC**：`spaces.artifacts.confirm`；`workspace.openFile`（路径经 `_isAllowedFileActionPath` 校验 → `shell.openPath` 系统默认应用打开，区别于 revealPath 的定位）。
- **前端** `workspace.js` 产物页：候选卡灰显 + 角标「待确认」+「确认产物」按钮；确认后强制重载变正式（打开按钮）；「来源任务」跳转会话；信息条文案区分 候选/正式。
- **测试**：spaces_artifacts.test.ts 4 用例（聚合 / 幽灵跳过 / AI 产出候选→确认正式 / 附件直接正式）。
- **CDP 实测**：word 先「待确认」→ 点确认 → 「文档」正式 + 打开按钮；后端 confirmed 翻转；打开返回 ok。

### 回归（F1）
- typecheck ✅；spaces_artifacts 4 过；spaces/chats 全过。
- 全量 test:js 失败文件清单与基线一致。
- 真实数据：「二千e q e」空间的 word 现为已确认正式产物（清单 `confirmed_artifacts.json` 保留）。

## 十四、追加：产物页两处 UX bug 修复（来源乱码 + 筛选空态卡死）

**日期**：2026-08-15（用户反馈「来源是一串乱码」+「该空间暂无产物但计数是 1」）

### G1｜来源显示 cid 乱码 → 任务标题
- `_mapArtifact` 的 `source`（cid，跳转用）与 `sourceTitle`（`_sessionTitleById(cid)`，展示用）分离；产物卡「来自：」显示任务标题（如「你好」），不再是一串 12 位 hex。`data-cid` 仍用 cid 供跳转。

### G2｜筛选空态把工具栏渲染丢了（用户被"卡"在筛选状态）
- **根因**：`_renderArtifactsPane` 在「筛选后 items 为空」时提前 `return` 纯空态，**没渲染筛选工具栏** → 用户切到「表格/网页」后切不回来（只能刷新），且 tab 计数（全部产物数）与空态文案（「暂无产物」）矛盾误导。
- **修复**：筛选工具栏常驻（`_renderArtifactsPane` 重构：note + toolbar 始终输出，空态在下方）；空态区分两种：`_artifacts` 真空 → 「该空间暂无产物」；有产物但筛选过滤 → 「没有符合「X」筛选的产物，切回「全部」查看」。另在 `_loadSpaceDetail`（切空间）重置 `_artifactFilter/_assetFilter` 为「全部」，避免跨空间残留。

### 验证（CDP，Page.reload 干净态）
- 来源显示「来自：你好」✓；切「表格」→ 空态提示（工具栏仍在）→ 切回「全部」→ 卡片恢复 ✓。
- 注意：验证时避免在同一 app 实例连续跑脚本（`_detailLoadedFor`/`_artifactFilter` 全局状态污染），用 Page.reload 隔离。
- 全量 test:js 失败文件清单与基线一致（无新增）。

## 十五、追加：产物漏收（工作区未登记文件 + 前端缓存不刷新）

**日期**：2026-08-15（用户「对话产出 4 个文件但产物里没有」）

### H1｜根因一：消息 produced[] 只登记了部分文件，工作区文件漏收
- **现象**：会话工作区 5 个文件（docx/pptx/xlsx/md/html），消息 produced 只记录 3 个；.md/.html 未登记（部分工具直接写文件、不走 onFileWritten 登记）。
- **修复**：`scanProducedFiles` 改为「消息 produced[]（已登记）+ 会话工作区目录兜底」两段扫描（`getConversationWorkspacePath` 解析落点，按文件名去重、附件优先）。
- **.html 放行**：`ALLOWED_EXTENSIONS` 刻意排除 html（附件上传防脚本）；工作区产物扫描单独放行 `.html/.htm`（AI 生成的网页，经「打开」走系统默认浏览器），不放松上传边界。

### H2｜根因二：产物 tab 用进入空间时加载的缓存，新产出不刷新
- `_detailLoadedFor` 同空间不重载（设计）；切到「产物/资产」tab 时强制 `_detailLoadedFor=null + _loadSpaceDetail` 重载，新产出/新确认立即可见。

### 验证
- CDP：产物 tab 显示 5 个文件（xlsx/html/pptx/md 待确认 + docx 正式）；切 tab 再切回仍 5 个。
- 测试：spaces_artifacts.test.ts 5 用例（新增「工作区兜底 md/html」；注意测试环境工作区根是 `T/userWorkSpace`，需用 getConversationWorkspacePath 解析真实落点）。
- 全量 test:js 失败文件清单与基线一致。

## 十六、追加：产物落位「严格审核」（保证空间会话产出都能进产物）

**日期**：2026-08-15（用户要求严格审核"空间会话产出的文件能否保证都落到空间产物"）

### 审核发现的 4 个漏洞 + 修复
| # | 漏洞 | 修复 |
|---|---|---|
| A | 工作区兜底扫描**不递归子目录**（AI 在 `output/` 等子目录产出的文件漏收） | `scanProducedFiles` 工作区段改为递归 `walk()`（跳过隐藏项） |
| B | **web artifact 只扫空间目录**；新产出的 web artifact（无 project 的会话落全局 `cloud/chat_artifacts/<cid>/`）漏收 | `scanArtifacts` 双目录（空间 + 全局 `chatArtifactCidDir`），按 artifactId 去重 |
| C | 扩展名覆盖不全（`.doc/.xls/.ppt/.svg/.zip/.epub/.pages` 等漏） | 新增 `PRODUCED_EXTRA_EXTS`（旧 Office / svg / 压缩包 / 电子书 / html），工作区/产物扫描用宽集合 `isProducedExt`；**附件上传白名单不变**（防脚本边界不放松） |
| D | `workspace.openFile` 校验只认消息 produced[]，工作区兜底扫到的**未登记文件打不开** | `_isAllowedFileActionPath` 增加：空间会话（`conv.space_id`）工作区目录内文件放行（`getConversationWorkspacePath` 前缀匹配）；越权路径仍拒绝 |

### 验证
- CDP：产物 tab 5 文件全显示；`workspace.openFile` 打开未登记 html ✓、`/etc/hosts` 越权拒绝 ✓。
- 测试：spaces_artifacts.test.ts 扩到 7 用例（新增「子目录 .svg/.doc 收」「全局 chat_artifacts web artifact 收」）。
- 全量 test:js 失败文件清单与基线一致。

## 十七、追加：产物按空间分开存放（工作区路径模型变更 + 历史数据迁移）

**日期**：2026-08-15（用户「产物按空间分开，不都堆一块」；智能体沉淀用户明确不做）

### I1｜路径模型：空间会话工作区进各自空间目录
- `paths.ts` 新增 `spaceWorkspaceDir(uid, sid)` = `<spaces>/<sid>/workspace`。
- `conv_workspace.ts::getConversationWorkspacePath`：空间会话（`conv.space_id` 有值）→ root = 空间工作区目录（`spaces/<sid>/workspace/<slug>`）；未绑空间会话保持 `userWorkSpace` 根。产物扫描/AI 工具 workingDir 自动跟随（都走该函数）。
- 空间 A 与空间 B 产出彻底隔离；删空间时随 `spaceContentDir` 一起删。

### I2｜历史数据惰性迁移（先备份后迁移）
- 备份：`/tmp/cogseed-devdata-backup/data-workspace-migrate-20260815-111021.tar.gz`（28M）。
- 迁移：`getConversationWorkspacePath` 检测空间会话旧工作区在 `userWorkSpace/<dir>` → 搬到 `spaces/<sid>/workspace/<dir>`（同盘 rename / 跨设备 copy+rm；幂等；失败告警不阻断）。
- **共享目录防串保护**：历史 slug 冲突（两会话同标题共用 `userWorkSpace/你好`，如 111 空间的 899e25ba73f1 产天津攻略 + 二千e q e 的 b4a2de42e84d 产周末去北京）→ 迁移时检测 legacy 目录含「非本会话 produced」文件 → 只搬本会话 produced 文件（不搬整个目录，避免把别空间产物搬错）。
- **真实数据修正**：迁移一度把共享「你好」目录整体搬去 111 空间（899e25ba73f1 先触发），已按 produced 归属拆回——天津攻略 4 个留 111、周末去北京 5 个回二千e q e。
- 验证：二千e q e 产物 = 周末去北京 5 个；111 空间 = 天津攻略 4 个，互不混；未绑会话工作区（空间模式/写报告/做成word和pdf）仍留 userWorkSpace。

### I3｜测试
- 新增 `conv-workspace-space.test.ts` 2 用例（空间会话 → 空间工作区路径 + 未绑 → userWorkSpace；历史工作区惰性迁移）。注意测试环境工作区根是 `getWorkspacePath(uid)`（T/userWorkSpace），legacy 文件要放对根。
- 全量 test:js 失败文件清单与基线一致（无新增）。

## 十八、追加：架构稳固性修复（方案 Y——归属变更工作区随迁，不丢文件）

**日期**：2026-08-15（用户质疑架构稳固性：主流"项目=文件夹"路径恒定，CogSeed 空间会话锚定但文件落位也锚 space_id → 归属可变会漂移）

### 深挖发现的两个不稳固点（已用测试验证）
1. **解绑/换空间 = 工作区路径漂移**：`setConversationSpace` 只改 `space_id`，工作区路径（`spaces/<sid>/workspace/<slug>`）随归属变但文件不搬 → 旧产物"消失"（文件还在旧目录，扫描扫新路径）。
2. **删空间 = 会话产出文件被物理删除**：`deleteSpace` 清会话 space_id（会话保留、落最近任务）后 `rm spaceContentDir` 删掉整个 `workspace/` → 会话在但历史产物永久丢失（与"会话落到最近任务"语义矛盾）。

### 修复（方案 Y：归属变更自动搬家）
- `conv_workspace.ts` 新增 `migrateConversationWorkspace(uid, cid, fromSpaceId, toSpaceId)`：按 `state.workspace_dir` 计算新旧落点（null=userWorkSpace，非空=空间目录）；同盘 rename、跨设备 copy+rm；幂等（源不存在/目标已存在跳过，防 slug 冲突覆盖）；失败告警。
- `chats.setConversationSpace`：space_id 变更时（绑定/解绑/换空间）await 迁移工作区 → 文件随归属走。
- `deleteSpace` 自动受益（内部调 `setConversationSpace(cid, null)` 先迁移，再 rm 空间目录 → 文件已搬走，删目录不删文件）。
- 语义：解绑/换空间 → 文件跟会话走；删空间 → 该空间会话的工作区全部迁到 userWorkSpace（会话保留产物，不丢），再删空间目录。

### 验证
- 测试 `conv-workspace-space.test.ts` 扩到 5 用例（新增：解绑迁 userWorkSpace、换空间 A→B 迁 B、删空间迁 userWorkSpace 且空间目录删后文件仍在）。
- CDP：真实数据不受影响（二千e q e 5 产物 / 111 空间 4 产物正常）。
- 全量 test:js 失败文件清单与基线一致（无新增）。

## 十九、追加：空间认知资产管线（接 recall，按空间显示）

**日期**：2026-08-15（用户：空间绘画产出 → 候选池 → 确认 → 正式资产 → 空间资产 tab 显示；资产随 recall 全局、不随空间删；显示只显示本空间产生的、空间可读全局）

### J1｜候选/资产加空间归属（spaceId）
- `candidate-service.ts`：`RecallCandidateRecord`/`RecallAbilityAssetRecord` 加 `spaceId?`（空间归属）；`SaveRecallCandidateInput` 加 `spaceId?`；save 写入、promote 时资产继承候选 `spaceId`。
- `asset-service.ts`：新增 `listAbilityAssetsForSpace(userId, spaceId)`（按 spaceId 过滤；资产随 recall 全局存储、不随空间删；空间可读全局走 listAbilityAssets）。
- `capture-service.ts`：`runRecallCapture` 的 saveRecallCandidate 调用透传来源会话 `space_id`（空间对话自动捕获的认知候选带空间归属）。

### J2｜修复现有 bug：IPC 提升候选一直失败
- `promoteRecallCaptureCandidate` 调 `promoteRecallCandidate` 未传 `{ actor: 'user' }`，而该函数强制要求 user actor → 现有 `recall.candidates.promote` IPC 提升必失败。已补 `{ actor: 'user' }`（两处）。

### J3｜空间资产 tab 显示沉淀资产
- `workspace.js`：`_loadSpaceDetail` 资产 tab 数据源从「引用绑定 spaces.assets.list」改为「recall.assets.listForSpace（按空间过滤的沉淀资产）」；新增 `_mapRecallAsset`（标题/类型/状态/成熟度）；空态文案改「该空间暂无沉淀资产…」。
- IPC：`recall.candidates.save` 透传 `spaceId`（校验 safeId）；新增 `recall.assets.listForSpace`。

### 验证
- 测试：`candidate-service.test.ts` 新增 2 用例（save 带 spaceId→promote→资产继承→listForSpace 过滤 / 无 spaceId 不进任何空间列表）。
- CDP 实测闭环：候选带 spaceId → promote（修复后 ok）→ 资产继承 spaceId → listForSpace 返回 → 空间资产 tab 显示「配色规范」。
- 测试残留已清理（cdp 资产/候选删净）。
- 全量 test:js 失败文件清单与基线一致。

## 二十、追加：全链路/UI 扫查修复（假按钮、mock 数据、断链）

**日期**：2026-08-15（用户扫查后逐项拍板）

### K1｜产物：确认文案改「确认」+ 新增「驳回」
- `spaces_artifacts.ts`：确认/驳回清单改为 `artifacts_state.json` = `{confirmed, rejected}`（**兼容回退**：新文件缺失时读旧 `confirmed_artifacts.json` 并迁移，用户已确认状态不丢）。
- 新增 `rejectSpaceArtifact`（驳回后不再作为候选展示）；`scanProducedFiles` 跳过 rejected。
- IPC `spaces.artifacts.reject`；前端候选卡 footer「确认」+「驳回」两个按钮。
- 示例：候选产物现在可「确认」转正式 /「驳回」隐藏。

### K2｜资产卡：撤销按钮接真（recall revoke）+ 删「已复用 0 次」
- 资产 tab 数据源改 recall 后，原「解绑」按钮（引用绑定接口）对 recall 资产无效 → 改为「撤销」（`recall.assets.revoke`，确认后资产变「已撤销」灰态，按钮隐藏）。
- 资产卡 footer 删「已复用 N 次」（写死 0 的假数据）。

### K3｜@ 引用资产源断链修复：改全局 recall 资产
- `_openRefPicker` 资产目录从「引用绑定 spaces.assets.list」（恒空）改为「recall.assets.list（全局）」，字段适配 `{asset_id,title,asset_type}`——空间可读全局资产（引用时），显示仍按各自来源。CDP 实测引用选择器资产 tab 显示 5 条全局资产。

### K4｜base_agent 扩展点（后续接其他 coding agent）
- `Space` 加 `base_agent?`；createSpace/updateSpace/IPC `spaces.create/update` 透传；新建空间弹窗「基础 Agent」下拉选中即保存；设置抽屉「当前对话 Agent」显示真实 `base_agent`（替代写死 Codex；无则默认 Codex）。

### K5｜删除死任务页（`_renderTask`）
- 任务页无入口 + 7 个 stub 假按钮（搜索/重新执行/更多/添加/调用专家/发送/面板设置）→ 整个页面删除；「新建任务」功能保留在任务 tab 底部内联输入条（真实创建空间会话）。

### 验证
- CDP：候选卡「确认/驳回」✓、用户确认状态兼容（周末去北京怎么玩.docx 仍正式）✓、资产撤销按钮 + 复用删 ✓、base_agent 显示 ✓、@ 引用资产全局 5 条 ✓。
- 测试：spaces_artifacts 7 / conv-workspace-space 5 / candidate 9 全过（2 个 recall 基线失败）。
- 全量 test:js 失败文件清单与基线一致。

## 二十一、追加：交接待办四项（角色上限 / 弹窗布局 / 场景入口 / @ 选择器空间化）

**日期**：2026-08-15（执行交接提示词的 4 个已确认待办，全部落地并 CDP 实测）

### L1｜新建空间角色模板上限 3 个（1 主 + 2 副）
- 现象：新建空间能力选择「角色」不限数量，后端 `_createSpace` 静默 `slice(0,2)` 截断无提示。
- 修复：`workspace.js::_toggleAbility` 在 `kind==='role'` 时限制——已选模板计 1 个角色，其余最多再勾 2 个（上限 3）；超限点击被拦并弹 `uiToast`「角色最多选择 3 个（1 主 + 2 副）」。
- 弹窗内新增 `ws-ability-limit-hint` 提示条（角色 tab 顶部常驻）。
- CDP 实测：自定义场景空选 → 依次勾 4 个角色，第 1~3 个成功、第 4 个被拦（sel 保持 3）。

### L2｜空间设置「调整 / 主技能」弹窗布局修复（无 nav 却占 176px 左列）
- 现象：`_renderEditAbilityModal` 与 `_renderMainSkillModal` 复用 `ws-ability-main`（grid 两列含 176px 侧栏）但没渲染 nav → 左侧空列、内容被挤右。
- 修复：两个弹窗加 `ws-ability-main-solo` 修饰类，`workspace.css` 覆盖为单列 `minmax(0,1fr)`（新建空间能力弹窗有 nav，保持双列不变）。
- CDP 实测：grid-template-columns 由 `176px …` 变为单列 `898px`。

### L3｜旧「场景」入口迁入新空间中心「从模板创建」
- 现象：旧面板 spaces.js 的场景入口（教育/写作/职场/自定义，含建议模板组合）没迁移。
- 修复：`_loadData` 增载 `spaces.scenarios.list`（后端 SCENARIOS 在 `role_templates.ts`，IPC 已注册）；`_renderCenter`「从模板创建」区新增「场景」组（4 张场景卡）+「角色模板」组并列；点场景 → `_openCreateFromScene` 预填主+副角色模板进新建弹窗（名称=场景名，≤3 个角色，不套单一模板 bundle）。
- CDP 实测：场景组 4 卡（education/writing/workplace/custom）+ 角色模板 9 卡并列；点「教育」→ 新建弹窗名称「教育」、角色 chips = 学生+学者。

### L4｜@ 选择器空间化改造（核心：按会话空间判定 tab 集）
- 现象：@ 五类 tab（智能体/技能/连接器/资料库/本体）对所有会话一样；空间会话 @ 与空间任务 composer 的 @（引用产物/资产）脱节。
- 方案（用户确认）：
  - `agents.js::_agentPickerSpaceId(anchorId)` 判定锚点作用域空间（chat=currentCid 的 space_id；new-chat=chip 选中空间；auto=无）；
  - `_agentPickerVisibleTabs`：空间会话 = 智能体/技能/产物/资产；无空间 = 智能体/技能；Auto = 智能体/技能；
  - **连接器/资料库/本体 tab 两处全删**（index.html 按钮移除 + `_AGENT_PICKER_TAB_ORDER` 收敛 + `_renderAgentPickerList` 分发删除 + search placeholder 删除 + `_ensureAgentPickerTabData` connectors 分支删除；底层 loader 函数保留死代码，测试直调不受影响）；
  - 产物/资产 tab：数据源 = `spaces.artifacts.list`（按空间）+ `recall.assets.list`（全局，同 workspace.js `_openRefPicker` 语义）；选中 → `conversations.taskRefs.add` 写入（**复用任务引用逻辑，发送时 groupChat.send 自动并入 AI 可读**）；
  - composer 引用条：index.html 加 `#chat-taskrefs`（conversation）+ `#new-chat-taskrefs`（new-chat）；conversation 条随 `onEnterConversationView` 刷新（空间详情「＋引用」写入的也显示）、chip × 移除调 `taskRefs.remove`；new-chat 选中进 pending，`handleNewChatSubmit` 建会话后 `commitNewChatTaskRefs(convId)` 写入再发送。
- 新增 i18n：`agent_picker.tab_artifacts/tab_assets`、搜索占位、空态、ref_added/remove 等（en/zh/ja/pt）。

### 验证
- CDP 实测（示例·产品设计空间临时会话，用完即删）：
  - 空间会话 @ = 4 tab（智能体/技能/产物/资产），连接器/资料库/本体按钮不存在 ✓；
  - 无空间（new-chat 默认工作区）@ = 2 tab ✓；new-chat 选空间后 @ = 4 tab ✓；
  - 产物选中「官网信息架构.md」→ task_references 写入（kind=artifact）→ composer chip 渲染 ✓；
  - 资产选中「竞品可比维度清单」→ task_references 写入（kind=asset, asset_id, asset_type）✓；
  - chip 跨视图切换持久显示、× 移除 → taskRefs.remove 归零 ✓；
  - 临时会话删除，示例空间 6 任务/8 产物/5 资产不受影响，无 cdp-* 残留 ✓。
- 测试：新增 `agent-picker-space-refs.test.ts`（7 用例：tab 判定/产物资产渲染/引用写入/new-chat pending+提交/chips）；category-tabs 3 处 tab 断言更新 + 新增空间 tab 判定用例；lazy-features 1 处断言更新（connectors 分支删除）；category-tabs 9 失败 / lazy-features 1 失败均为既有基线。
- typecheck ✓、smoke ✓、全量 test:js 失败文件清单与基线一致（20 文件 / 73 failed / 7687+ passed）。

## 二十二、追加：空间详情对话框统一为标准会话对话框 + 空间构建师对接新管线（副模板）

**日期**：2026-08-15（用户反馈：空间详情页的内联开新任务输入条有问题；右上角「新建任务」应一键跳到标准会话页；两页面对话框统一用标准会话对话框；检查空间构建师是否对接新工作空间管线）

### M1｜空间详情任务 tab 去掉内联「开新任务」输入条
- 删除 `_newTaskComposerHtml`（＋附件 / @ 引用 / 输入条 / 发送 的内联 composer）及配套：`_spaceDraftCid`、`space-attach` 附件宿主、`ref-picker-open`（新建任务引用选择器）、`new-task-input/send`、`pending-ref-remove/clear`；对应 workspace.css 的 `.ws-new-task-*` / `.ws-ref-chips` 规则一并删除。
- 任务 tab 只留任务列表；空态加提示「点右上角『新建任务』在标准对话框里开始第一项任务」。

### M2｜右上角「新建任务」一键跳到标准会话对话框
- `_startNewTask(spaceId)` 改为：`conversations.create {spaceId}`（无标题，首条消息后 autoTitle）→ 同步侧栏缓存 → 刷新空间任务列表 → `setView('conversation', cid)`。
- 新任务从落地起就用**与主对话同一个 composer**（+ 附件 / To / 工作空间 chip / @ 引用产物资产 / 发送）——两页面对话框完全一致；新任务引用/附件都在标准对话框里完成（配合第二十一章 @ 选择器产物/资产 tab）。

### M3｜空间构建师对接新管线：草稿支持副模板（secondary_template_ids）
- 排查结论：构建师 prompt 已注入新目录（skills/agents/templates/scenarios），草稿 → `spaces.createFromDraft` → `createSpaceFromDraft` 已走新管线（模板存在性/bundle 派生/技能智能体校验）；**缺口 = 草稿 schema 只有 primary_template_id，没有副模板**（新管线支持 1 主 + 2 副）。
- 修复：
  - 后端 `spaces.ts::SpaceDraft` 加 `secondary_template_ids?`；`createSpaceFromDraft` 校验每个副模板存在（≤2、去重排除主模板），副模板 bundle 并入去重集合（与 `resolveSpaceResources` 主+副 bundle ∪ extra 语义一致），创建时透传。
  - prompt `space_builder.md`：草稿 schema 示例加 `secondary_template_ids` + 使用规则（横跨多角色时才补 1-2 个副模板）。
  - 渲染层：草稿卡加「副模板」行（id → 中文名）；`_createSpaceFromDraft` 透传 `secondary_template_ids`。
  - i18n：`new_chat.space_draft_secondary`（en/zh/ja/pt）。

### 验证
- CDP 实测：空间详情任务 tab 无 `.ws-new-task-bar` / `new-task-input` / `ref-picker-open` / `ws-attach-host`；点「新建任务」→ view=conversation、标准对话框（chat-input + recipient chip + workspace chip）就位 ✓。
- CDP 端到端（构建师草稿）：`spaces.createFromDraft` 带 `secondary_template_ids:['project_manager','fde']` → 空间落盘 primary=product_manager + secondary 两枚 ✓；`spaces.list` meta `template_names = 产品经理 项目经理 FDE 交付` ✓；验证完删除临时空间，零残留。
- 测试：spaces-p3394 新增 2 用例（主+副落盘 / 副模板不存在或超 2 个 → invalid_draft 不落盘），17 全过。
- typecheck ✓；全量 test:js 失败文件清单与基线一致。

## 二十三、追加：空间对话 @ 产物/资产「发不出去」修复 + @ 资产改空间资产

**日期**：2026-08-15（用户反馈：空间里的对话 @ 产物和资产发不出去；@ 的资产应是空间资产，现在是全局资产）

### N1｜根因：标准 composer 发送路径从未合并 task_references
- 渲染层标准对话走 `conversations.sendStream`（IPC 流式，`POST /api/conversations/{cid}/send/stream`），它直接调 `groupChat.send` 核心；带「task_references 合并」的只有 `groupChat.send` IPC（非流式 `/send`，UI 不用）→ @ 选中的产物/资产永远到不了模型 = 「发不出去」。
- 修复：合并逻辑下沉到 **`group_chat/index.ts::send()` 核心**（所有发送路径的汇聚点：sendStream / groupChat.send IPC / 队列重放）：
  - `_mergeTaskReferences(userId, cid, text, refs, modelText)`：产物 → references（经跨任务引用解析），资产 → model_text 上下文块（不污染可见文本）；
  - `_resolveTaskArtifactSourceMessage` 从 IPC 层迁入（同时**修复 produced 只认 `{path}` 对象、不认字符串全路径**的漏配——AI 产出文件落盘时 produced[] 是字符串全路径，导致产物引用被静默丢弃）；
  - IPC `groupChat.send` 改为纯透传（删重复合并 + 删旧 `_resolveArtifactSourceMessage`）。

### N2｜@ 资产改为空间资产
- `agents.js::_loadAssetPickerRows(spaceId)` 数据源 `recall.assets.list`（全局）→ **`recall.assets.listForSpace {spaceId}`**（本空间沉淀资产，与空间资产 tab 同源）；渲染函数加无空间守卫；
- workspace.js 任务引用选择器（`_openRefPicker`）同步改 listForSpace，保持一致；
- 组标签 i18n「全局资产」→「空间资产」（en/zh/ja/pt）。

### N3｜产物/资产 tab 卡「加载中…」修复（renderSeq 被外部递增失效）
- 现象：点产物/资产 tab 后偶尔永久停在「加载中…」——`_openAgentPicker`/`_refreshAgentPickerProjectContext` 在异步作用域解析完成后递增 `_pickerArtifactRenderSeq`/`_pickerAssetRenderSeq`，使在途加载的 `.then` 渲染守卫（renderSeq 不一致）跳过且不再重绘。
- 修复：移除两处外部 renderSeq 递增（保留缓存重置），渲染函数改以 `_agentPickerOpenSeq` 会话守卫（过期结果丢弃 + 过期渲染跳过），renderSeq 只由渲染本身递增。

### 验证
- CDP 端到端（示例·产品设计空间，临时会话用完即删）：
  - @ 资产 tab 显示本空间沉淀资产（竞品可比维度清单 / 信息架构四层法），组标签「空间资产」✓；选中 → taskRefs(kind=asset) → chip ✓；发送后 user 消息 `model_text` 含「【本任务引用的空间资产】- 竞品可比维度清单」✓；
  - @ 产物 tab 显示空间产物；选中「官网信息架构.md」→ taskRefs(kind=artifact, source_cid, file_name) → 发送后 user 消息 references 含源消息引用（produced 路径透传，模型上下文 files 可定位）✓；
  - 资产 tab 不再卡「加载中…」（renderSeq 修复后稳定出列表）✓。
- 测试：新增 `group_chat/task-reference-merge.test.ts`（3 用例：产物→references+资产→model_text / produced 字符串路径解析 / 无引用回归守卫）；`agent-picker-space-refs.test.ts` 扩到 8 用例（空间资产渲染 + listForSpace 调用断言）；spaces-p3394 17 用例不变。
- typecheck ✓、smoke ✓、全量 test:js 失败文件清单与基线一致。

## 二十四、追加：@ 发送后 UI 交互反馈（chip 清除 / 占位符 / 气泡引用条）

**日期**：2026-08-15（用户反馈：@ 产物/资产后发送，composer 无任何变化——chip 还挂着、占位符还写「连接器、资料库」，交互没有反馈）

### O1｜发送后清空 composer 引用条
- `agents.js::clearChatTaskRefChips()`：隐藏 `#chat-taskrefs` / `#new-chat-taskrefs`（服务端 task_references 保留——任务级引用仍持久，空间任务行「引用 N」徽标与 ＋引用 不受影响）。
- `conversation.js` handleChatSubmit / handleNewChatSubmit：发送前调用 → composer 立即复位（chip 消失 = 发送生效的视觉反馈）。

### O2｜composer 占位符去旧 + 随空间动态
- 静态占位符去掉「连接器、资料库」（已删 tab）：`chat.input_placeholder` / `new_chat.input_placeholder` 改为「输入 @ 选择智能体、技能」（en/ja/pt 同步）。
- `agents.js::updateAgentPickerPlaceholders()`：空间会话 → 「输入 @ 选择智能体、技能、产物、资产」，非空间 → 「输入 @ 选择智能体、技能」；**同时同步富文本编辑器**（contenteditable 镜像 `data-placeholder`，见 `_initMentionMirror`，只改 textarea.placeholder 不改编辑器则可见占位符不变）与 attribute（i18n 重应用/sync 读 getAttribute）。
- 触发点：`onEnterConversationView`（会话切换）、`user-workspace` 空间 chip 变更（conversation 绑定/解绑、new-chat 选空间）。

### O3｜发送后 user 气泡即时显示引用反馈
- 排查发现：controller 发送路径（hasLiveController）不触发历史 reconcile，实时气泡永远不显示服务端 references（手动 `_scheduleHistoryReconcileAfterStream(force)` 后正常渲染引用 bundle——渲染本身没问题）。
- 修复：`_makeConvChatController::onUserAppended` 注入 `_injectUserTaskRefFeedback`——发送后异步拉当前会话 task_references，在 user 气泡顶部插入轻量「引用：产物 · 官网信息架构.md」条（`.chat-taskref-inline`），**即时反馈、不依赖 reconcile**；历史重载后服务端完整引用 bundle 会替换它。
- 资产可见反馈增强：`groupChat.send` 合并时把资产引用落 user 消息 `space_asset_refs` 字段（EnqueueParams/GroupMessage 扩展），渲染层 `_groupMsgToLegacy` 透传 + 气泡渲染「引用资产」chips（`.chat-space-asset-refs`）+ reconcile sidecar 检查。

### 验证
- CDP 端到端（示例·产品设计空间，临时会话用完即删）：
  - 空间会话可见占位符 = 「输入 @ 选择智能体、技能、产物、资产」（富文本编辑器 data-placeholder 同步）✓；非空间（new-chat 默认工作区）=「输入 @ 选择智能体、技能」✓；
  - @ 产物 → chip 显示 → 发送后 chip 立即清除（display:none）✓；
  - 发送后 user 气泡即时出现「引用：产物 · 官网信息架构.md」内联条 ✓（无 reconcile 依赖）；
  - 历史重载后气泡显示完整引用 bundle（文件 chips）✓。
- 测试：task-reference-merge 3 用例（+space_asset_refs 断言）、agent-picker-space-refs 8 用例、chat-use-inline-chips 9 用例全过。
- typecheck ✓；全量 test:js 失败文件清单与基线一致。

## 二十五、追加：主链路全绿复查（空间创建→…→@ 按空间过滤，逐环节 CDP 走查）

**日期**：2026-08-15（用户问：主链路各环节是否有变动/回归。用临时空间逐环节实测，验完即删、临时资产撤销）

| 环节 | 实测结果 |
|---|---|
| 空间创建（主+副模板/指令/基础Agent） | ✅ `spaces.create` 落盘 primary+secondary，spaces.list meta 正常 |
| 任务绑定（conversations.create spaceId / chip setSpace） | ✅ conv.space_id 绑定；解绑（默认工作区）→ space_id 清空（方案Y随迁） |
| AI 产出（工作区落位） | ✅ 产物写 `spaces/<sid>/workspace/<slug>/`（bus 首活跃冻结 workspace_dir 后扫描同路径） |
| 产物扫描/确认 | ✅ `spaces.artifacts.list` 扫到候选（source=produced, confirmed=false）→ confirm 后 confirmed=true |
| 认知候选 | ✅ `recall.candidates.save`（需 evidence sourceRefs 非空 + spaceId）→ pending, 带 spaceId |
| 沉淀资产 | ✅ promote 后资产继承 spaceId → `recall.assets.listForSpace` 返回（status active） |
| 侧栏分组 | ✅ 空间折叠组（组名=空间名、计数=会话数），会话行在组内 |
| chip 选空间 | ✅ workspace chip 显示空间名；绑定/解绑正常 |
| @ 按空间过滤 | ✅ 空间会话 @ = 智能体/技能/产物/资产 4 tab；resolveSpaceScope 作用域 agents=6/skills=11；资产 tab = 本空间资产（listForSpace） |

- 首轮走查 3 处"红"均为**测试顺序产物**（非应用回归）：① 预建工作区目录导致 slug 被 uniquify 成 `-2`（真实流程 bus 先冻结 workspace_dir 再写文件，扫描走冻结路径）→ 修正为先触发一次 artifacts.list 冻结再写文件；② 新空间没刷侧栏缓存导致组名显示 space_id → `invalidateSidebarSpaces` 后组名正常；③ chip 标签读取时机在 currentCid 切换前 → 切到目标会话后正常；④ 候选资产按 judgment 搜错（title 取 summary）→ 按 summary/candidateId 搜到。
- 测试：spaces / conv-workspace-space / spaces_artifacts / task-reference-merge 全过；candidate-service 仅 2 个既有基线失败（promotion 状态恢复类），spaceId 用例过。
- 数据零残留（临时空间/会话全删、临时资产撤销、上一轮孤儿资产已清理）。

## 二十六、追加：空间详情页「返回空间中心」按钮

**日期**：2026-08-15（用户反馈：空间详情页少一个返回上级的按钮）

- `_renderSpace` 头部新增 `.ws-space-back-row`「← 返回空间中心」按钮（`data-ws="back-to-center"`，chevron-left 图标 + i18n `ws.back_to_center`，workspace.css 加 `.ws-back` 样式）。
- 绑定：点击 → `_go('center')` 回到空间中心列表（重置设置抽屉/弹窗状态）。
- CDP 实测：空间详情页按钮存在（文案「返回空间中心」），点击后回到空间中心（标题「空间中心」）✓。

## 二十七、追加：合并后 index.html 丢失 @ 选择器 4-tab 与引用条容器（已补回）

**日期**：2026-08-15（用户反馈：合并后运行版本丢了好多功能——@ 产物/资产 tab 不见、@ 选中无 chips）

- 根因：用户合并提交时 **index.html 取了远端新版**（`git log` 见 215bbf3b「index.html 取远端新版删项目残留」），把本会话对 index.html 的 3 处改动覆盖掉：① @ 选择器 4-tab（智能体/技能/产物/资产，删连接器/资料库/本体）回到旧 5-tab；② `#chat-taskrefs` / `#new-chat-taskrefs` composer 引用条容器缺失。而 agents.js/workspace.js/主进程合并/测试/CSS/locales 均已随提交保留。
- 修复：在当前 index.html 上重新应用 3 处——选择器 tabs 换为 4 按钮（artifacts/assets 默认隐藏）、两个 composer overlay 补 `.chat-taskrefs` 容器。
- 验证：重启后 CDP 实测——空间会话 @ = 4 tab 全显、产物 tab 列出 6 个空间产物、选中「官网信息架构.md」→ `#chat-taskrefs` 渲染 chip ✓；返回按钮/场景/新建任务跳转等其余功能正常。
- 当前工作区仅剩 index.html 一处改动（+4/-3），可单独提交。

## 二十八、追加：合并丢失 Recall 引用反馈 UI（recallCitationsHtml 未定义）已恢复

**日期**：2026-08-15（用户报「加载失败: recallCitationsHtml is not defined」）

- 根因：与上一章同源——合并时 conversation.js/style.css/icons.js 取远端版本，把本地特有的 **Recall 引用反馈 UI**（「提供给本次回答的记忆」+ 有帮助/需改进）整体覆盖丢失：`_renderRecallCitationsHtml`/`_hydrateRecallCitations`/`_recallCitationTypeLabel`/`_recallCitationScopeLabel` 函数、`brain-circuit`/`thumbs-up`/`thumbs-down` 图标、`.chat-recall-citations*` 样式、气泡 hydrate 调用点。仅模板里残留 `recallCitationsHtml` 引用 → ReferenceError。
- 修复（按原实现恢复，后端 `recall.usage.feedback` IPC 与消息 `recall_citations` 透传均在）：
  - conversation.js：补回 4 个函数 + 气泡渲染后 `if (recallCitationsHtml) _hydrateRecallCitations(msgDiv, attachmentCid, message._msg_id);`
  - icons.js：补 `thumbs-up`/`thumbs-down`/`brain-circuit` 图标
  - style.css：补 `.chat-recall-citations*` 样式
- 验证：重启后 CDP 实测——4 个函数均在、打开真实会话（官网信息架构梳理，3 气泡）正常渲染、无加载错误/错误气泡；@ 4-tab、引用条、空间中心等上一章恢复的功能不受影响。
- 当前工作区共 5 处改动：index.html(+4/-3) / conversation.js(+98) / icons.js(+3) / style.css(+76) / 验收记录(+9)，可一并提交（合并丢失功能恢复）。

## 二十九、追加：空间构建师 prompt 借鉴 grill-me（一轮问清 + 推荐答案，控制轮次）

**日期**：2026-08-15（用户：空间构建师再借鉴 github.com/mattpocock/skills 的 grill-me skill，取其精华，但对话轮次不要太长）

- grill-me/grilling 精华：① 一轮把「当前能问的全部问题（frontier）」一次抛出，编号 + 推荐答案，用户一次确认/调整；② 事实类信息 AI 自己查/用注入清单，不问用户；③ 达成共识即出结果，不连环追问。
- 改造 `space_builder.md`：由「一次只问一个问题、来回多轮」改为「**1 轮问清、最多 2 轮出草稿**」——第一轮一次问 Q1 长期目标 / Q2 谁用+产出 / Q3 主模板（+可选副模板，直接推荐模板名）/ Q4 主技能或额外能力（推荐或"暂不需要"），每题附 ➡️ 推荐答案；事实用注入清单自己查，绝不反问"你有哪些模板/技能"；答案模糊就采纳推荐推进；只有影响模板选择的关键点才追加 1 轮，问完即出 space-draft。
- 草稿 schema 与输出规则（含 secondary_template_ids、严格 JSON、确认后交系统）保持不变。
- 验证：smoke 通过；space_system_prompt_inject 4 用例过；占位符 $skills_block/$agents_block/$templates_block/$scenarios_block 完好（loader 按 mtime 缓存，下次空间构建师对话自动生效）。

## 三十、追加：空间构建师草稿 id 幻觉修复（创建空间 invalid_draft）——沉淀为智能体检查

**日期**：2026-08-15（新 prompt 实测：构建师一轮问清+自动出草稿成功，但点「创建空间」失败 `invalid_draft: 主技能不存在或已禁用: solution-design；技能不存在或已禁用: office-word`）

- 根因（LLM 幻觉/用名当 id）：真实技能目录是十六进制 id（如 office-word 实际 id 是 `c72c656eca12`，126 个技能里根本没有 `solution-design`）。构建师把**显示名当 id**（office-word）且**幻觉出不存在的 id**（solution-design，疑似看到 `v0.2.1-review.3` 版本号拼凑）。后端 `createSpaceFromDraft` 的严格校验正确拦截（这正是它的设计目的）。
- 修复：`space_builder.md` 新增「**草稿 id 铁律**」——id 一律用注入清单里 `id: ` 后的十六进制、绝不用显示名（举例 office-word→c72c656eca12）；清单里没有的绝不出现在草稿（宁缺毋滥留空）；version 抄清单版本号去掉展示用 `v` 前缀；main_skill_ref/extra_skill_ids/extra_agent_ids 同理；出草稿前自检每个 id 能在注入清单找到。
- 回归守卫：新增 `test/main/prompts/space-builder-prompt.test.ts`（3 用例：注入占位符 / 短轮次引导 / id 铁律含示例锚点），防再次修改/合并丢失。
- 验证：smoke 过；space-builder-prompt 3 + space_system_prompt_inject 4 全过；prompt 按 mtime 缓存，重启后下一次空间构建师对话生效。用户可继续原会话说"用真实 id 重新出草稿"或新开空间模式重试。

## 三十一、追加：createSpaceFromDraft 容错自动纠正（不再 invalid_draft 卡死，根治 LLM 幻觉 id）

**日期**：2026-08-15（用户实测第二次仍失败同款 invalid_draft；要求"以后不要出现类似问题"）

- 结论：prompt 加固（第三十章 id 铁律）拦不住 LLM 用显示名当 id / 拼凑 id——`solution-design` 其实是真实技能的**显示名**（真实 id 464e3f3416cf），`office-word` 同理（c72c656eca12）。后端严格拒绝让用户卡死。
- 根治：`spaces.ts::createSpaceFromDraft` 改**容错自动纠正**：
  - 资源引用（模板/技能/智能体）先 **id 精确匹配 → 显示名模糊匹配**（忽略大小写/空白/分隔符），解析成功用真实 id（主技能 version 取目录实际版本号）；
  - 解析不到 → **忽略该项 + `corrections` 说明**，不阻断创建；
  - 仅结构错误（空名/重名/space_type 非法/目标超长）仍拒绝；
  - 成功返回带 `corrections`（主+副模板/主技能/extra 技能/extra 帮手都会记录"已按名称解析/已忽略"）。
  - IPC `spaces.createFromDraft` 透传 corrections；前端 `_createSpaceFromDraft` 返回 `{space, corrections}`，成功 toast 追加纠正说明。
- 测试：spaces-p3394 更新为断言"自动纠正+创建"（模板/主技能/extra 技能不存在 → 忽略+correction+落盘；副模板超 2 个 → 取前 2；模板按显示名解析 fde/software_engineer），18 用例过。
- 实测（用用户那份失败草稿）：`solution-design`→464e3f3416cf、`office-word`→c72c656eca12，主模板 fde，空间创建成功，corrections 返回，临时空间已删。
- 验证：typecheck 过；spaces-p3394/spaces/task-reference-merge/space-builder-prompt 53 用例过；smoke 过。

## 三十二、追加：空间构建师 prompt 定位修正——只聊需求，不问资源清单

**日期**：2026-08-15（用户明确：空间构建师的作用是用户不需要看技能/模板/智能体清单，在对话中深入了解需求 → 后台自动组装 → 给用户确认创建）

- 修正 `space_builder.md` 引导部分：
  - 第一轮只问需求（3 问，去掉原"主模板选哪个/要不要配技能帮手"两问）：① 长期反复做什么（核心）② 产出什么样+给谁 ③ 多久一次/量级（可选）；
  - **模板/技能/帮手的挑选是后台的事**——构建师从注入清单自动组装，绝不问用户选哪个模板/配什么技能，也不让用户做任何清单选择；
  - 出草稿用大白话解释"主工作台/副工作台/核心能力/帮手"各负责什么（用户听得懂，不罗列内部资源名）；
  - 推荐答案可参考回忆/旧项目作贴合猜测，但明确"这只是猜测，你真正想做什么？"，用户说"不是/太偏了"立刻跟用户方向走；
  - 保持：一轮问清（最多 2 轮出草稿）、事实自己查、id 铁律、draft JSON 格式。
- 验证：space-builder-prompt 守卫 3 用例过（占位符/短轮次/id 铁律）；prompt 按 mtime 缓存，下次空间构建师对话生效。

## 三十三、追加：空间构建师 prompt 还原为借鉴 grill-me 之前那版

**日期**：2026-08-15（用户决定：grill-me 版不合适，还原为借鉴之前那版）

- `space_builder.md` 整体还原为 grill-me 之前的基线：一次一个问题、最多三问（长期做什么 / 给谁用产出什么样 / 多久一次）、模糊就给默认不追问到底（"propose a sensible default space and move on. Do not interrogate."）；draft JSON 输出契约（含 secondary_template_ids）与注入占位符保持不变。
- id 幻觉问题不再依赖 prompt 铁律——已由第三十一章 `createSpaceFromDraft` 后端容错自动纠正兜底（id 精确→名字模糊→忽略+corrections）。
- 守卫测试 `space-builder-prompt.test.ts` 改为断言还原后的基线（占位符 / 一次一个问题+三问 / draft JSON 契约），3 用例过。
- 已重启生效（prompt 按 mtime 缓存）。

## 三十四、追加：认知资产六 tab 全部同显修复（display:flex 压掉 [hidden]）

**日期**：2026-08-15（用户：认知资产界面点上面六个 tab 没反应、全是显示同一个页面；提示看 spec.md——即认知资产链文档对应的这套 UI）

- 现象：六个 tab（总览/沉淀任务/能力资产/数据来源/我的能力/关于我）点击无视觉反应。
- 根因：JS 切换逻辑正确（`switchSkillsCognitionPage` 正确设置 `hidden` 属性），但 `style.css` 里 `.skills-cognition-page { display: flex; }` **覆盖了 UA 样式表 `[hidden]` 的 `display:none`** → 六个页面全部同时显示（实测 hiddenAttr=true 但 computed display=flex、rectH=758）。代码库其他 flex 元素都有 `[hidden]{display:none}` 覆盖，唯独 `.skills-cognition-page` 漏了（合并时远端 style.css 加 display:flex 引入的回归）。
- 修复：`.skills-cognition-page[hidden] { display: none; }`（recall-local.css + style.css 各补一条，更高优先级压回 flex）。
- 验证：重启后实测——非活动 tab display:none/rectH 0，活动 tab flex/758；六个 tab 逐点内容各不相同（总览/沉淀任务/能力资产/数据来源/我的能力/关于我）。

## 三十五、追加：空间配置草稿卡实时对话不显示修复（切页回来才显示）

**日期**：2026-08-15（用户：空间构建师出的配置草稿卡对话中看不见，切到其他页面再切回来才显示）

- 根因：草稿卡（`space-draft-card`）只在历史重载路径 `appendChatMessage`（`_renderMessageHtml` 里 `_extractSpaceDraft` → `spaceDraftHtml`）渲染；**实时流式 finalize 路径 `_finalizeActorPlaceholder` 只更新 markdown 正文 + 各类 chip/卡片（created-agent/skill、form、expense、marketplace），从不渲染 space-draft 卡** → 对话中看不到、切页回来（历史重载）才显示。
- 修复：`_finalizeActorPlaceholder` 在最终正文落定后，若气泡内无草稿卡且 `gm.text` 含 ```space-draft 块，则 `_extractSpaceDraft(text)` → `_renderSpaceDraftButtonHtml` → append 进 `.chat-bubble`（与 appendChatMessage 逻辑一致；创建按钮走既有全局委托点击，无需额外绑定）。
- 验证：历史路径回归（打开含草稿会话 7a9418af6fef，草稿卡 1 张、内容正确、无报错）；renderer 测试 agent-picker-space-refs 8 + category-tabs 34 全过。

## 三十六、全量冒烟 + 全量 test:js（合并后首次全量）

**日期**：2026-08-15（用户：全量跑个冒烟测试）

- smoke：全过（paths 8 / storage 8 / prompts 7）。
- 全量 test:js（合并后 723 文件 / 8445 用例）：21 failed / 8401 passed（13 文件）。
  - **我改动的核心文件全 0 失败**：spaces-p3394 / spaces / task-reference-merge / space-builder-prompt / agent-picker-space-refs / conv-workspace-space / spaces_artifacts / chat-use-inline-chips。
  - **conversation-recall-citations（合并冲突，已修）**：远端测试断言"不渲染 Recall 引用 footer"，本地产品决定是保留（用户实际在用的「有帮助/需改进」）；按保留版更新断言，通过。
  - 其余 12 文件为**合并引入或既有基线**（与本会话改动无关）：lazy-features×2（合并把 spaces 特性改名 workspace 测试没跟上 / Settings bundle 顺序）、cogseed-residual-identifiers（CLAUDE.md 标识）、skills-frontmatter×2、skills-declaration-check×2、custom_providers、hub_account/deep-link×3、kstar/requirement-closure、local_agents/active_config_storage、builtin-resource-gate×4、codesign-runtime-gate、recall/capture-service、conversation-sidebar。
