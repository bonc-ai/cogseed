语音面板仅调整现有录音控件的外观和位置：麦克风入口上方 6px、宽 280px，复用 Popover，dismissible=false。面板只显示波形、正在聆听和取消操作，转写直接显示正文。再次点击麦克风结束录音，沿用最终结果回填与清理流程；取消清空转写。

不新增独立的结束识别中状态、面板结束按钮、录音期间发送锁或错误浮层重试入口。权限和设备错误继续使用现有提示框；组件页只展示对应文案。预览不访问麦克风或转写服务。
