独立排队悬浮表面，放在输入台上方并间隔 6px。条目保留编辑、保存、删除与拖动排序，不新增上移／下移按钮。编辑为多行，Ctrl / Cmd + Enter 保存，Escape 取消，保存空内容即删除。所有快捷键跳过 IME composition。

执行中按钮停止当前回复，Enter 入队。当前会话回复结束（包括中止）后仍尝试发送下一条；停止不表示暂停队列。组件页点击停止模拟开始下一条，不调用真实服务或实现另一套队列调度。

状态审阅入口见 Composer.prompt.md。
