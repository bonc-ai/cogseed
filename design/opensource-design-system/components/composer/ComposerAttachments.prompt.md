附件为单行横向滚动标签，高 32px，单项最高宽 220px。仅显示文件图标、文件名和加载／删除控件；不显示大小、状态文案或大缩略图，不折行、不折叠前三项。完整名称和视频仅供查看说明放入悬停提示。上传中不可移除，提交时沿用现有等待提示，不新增发送按钮提前禁用规则。

实际上传失败路径移除临时附件，通过现有错误提示说明原因，用户通过＋重新添加。组件的 error 渲染能力仅兼容已有数据，不作为上传流程必经状态或本轮新增需求；组件页的失败提示为文案展示，不改变客户端提示框形式。
