Input — 高 36（紧凑 32）、圆角 9、焦点使用中性灰边线，不加品牌色描边或外环。

```jsx
<Input placeholder="项目台账关键词" />
<Input icon="search" placeholder="搜索任务、产物与空间" suffix="⌘K" />
<Input mono defaultValue="2026-13-01" invalid />
```

占位符写「要填什么」，不写「请输入…」。错误态绝不把整行铺红。