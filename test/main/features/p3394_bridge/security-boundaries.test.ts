import { describe, expect, it } from 'vitest';
import type { Agent } from '../../../../src/main/features/agents';
import { buildP3394BridgeManifest, P3394BridgeKernel, validateP3394ExternalAdapterDescriptor, P3394WebSocketChannel } from '../../../../src/main/features/p3394';
function manifest(id:string){const r=buildP3394BridgeManifest({agent_id:id,name:id,description_zh:'',description_en:'',workflow:'',category:'general'} as Agent); if(!r.ok) throw new Error(r.error.message); return r.manifest;}
describe('P3394 bridge security boundaries',()=>{it('fails closed on unknown peer, disabled network, and capability mismatch',async()=>{const bridge=new P3394BridgeKernel(); bridge.registry.register({identity:{agent_id:'a',display_name:'A'},manifest:manifest('a')}); const envelope={spec_version:'p3394/1.0',message_id:'m1',session_id:'s1',kind:'task',performative:'request',sender:{agent_id:'a'},recipients:[{agent_id:'missing'}],payload:{parts:[{type:'text',text:'do'}]},idempotency_key:'idem'} as any; expect(bridge.send(envelope)).toMatchObject({ok:false,error:{reason:'peer_not_found'}}); await expect(new P3394WebSocketChannel({enabled:false}).listen()).rejects.toThrow(/disabled/); expect(validateP3394ExternalAdapterDescriptor({id:'mcp',kind:'capability',endpoint:'mcp://x',authorized:true,capabilities:['autonomous-agent']})).toMatchObject({ok:false});});});
