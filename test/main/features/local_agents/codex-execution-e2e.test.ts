import { afterAll, beforeAll, describe, expect, it } from 'vitest';
import * as fs from 'node:fs'; import * as os from 'node:os'; import * as path from 'node:path';
const enabled = process.env.COGSEED_RUN_REAL_CODEX === '1';
let root='';
beforeAll(async()=>{root=fs.mkdtempSync(path.join(os.tmpdir(),'real-codex-'));process.env.COGSEED_WORKSPACE_ROOT=root;process.env.COGSEED_BRIDGE_DISABLED='1';const users=await import('../../../../src/main/features/users');const paths=await import('../../../../src/main/paths');users.activateUser('real-codex-smoke');const file=paths.agentDefinitionFile('real-codex-smoke','agent-smoke');fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,JSON.stringify({agent_id:'agent-smoke',runtime:{kind:'cli',cli:'codex'}}));});
afterAll(()=>{delete process.env.COGSEED_WORKSPACE_ROOT;delete process.env.COGSEED_BRIDGE_DISABLED;fs.rmSync(root,{recursive:true,force:true});});
describe.skipIf(!enabled)('real Codex execution (set COGSEED_RUN_REAL_CODEX=1)',()=>{it('returns a real thread id and terminal result',async()=>{const runner=await import('../../../../src/main/features/local_agents/runner');const events:any[]=[];const result=await runner.run({uid:'real-codex-smoke',cid:'conversation-smoke',agentId:'agent-smoke',cli:'codex',prompt:'Reply with exactly: COGSEED_CODEX_OK',cwd:root,signal:new AbortController().signal,onEvent:e=>events.push(e)});expect(result.status).toBe('completed');expect(result.sessionId).toEqual(expect.any(String));expect(events.some(e=>e.type==='done')).toBe(true);},120000);});
