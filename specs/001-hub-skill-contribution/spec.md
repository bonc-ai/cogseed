# 功能规格：开源版迭代二｜客户端提交与社区贡献（客户端侧）

**Feature Branch**: `feat/iter2-client-contribution`

**Created**: 2026-09-18

**Status**: Draft — 客户端侧工程规格，派生自 Hub 侧产品契约 `PV-HUB-OPEN2-001`（`HANDOFF-HUB-OPEN2-001` 于 2026-09-18 获 Owner 接受）。尚未 Plan、Build、验收或发布。本文与 Hub 侧规格发生冲突时以 Hub 侧规格为准，并通过 Change Candidate 回改。

**Input**: MeshSeed 任务 `COGSEED-313`【需求】开源版迭代二：客户端提交与社区贡献 → 子任务 `COGSEED-313.1` 需求 PRD 编写（负责人 樊中扬，阶段 `OpenSource-Hub-v1.3.0`；评论 #89 给出客户端范围、不做清单、依赖与待对齐接口）；Hub 侧规格 cogseed-hub `specs/009-client-contribution/spec.md`（分支 `feat/iter2-client-contribution`）；附件《CogSeed 社区贡献条款 v1.0》正式稿；Owner 2026-09-18 决定：登录即可投稿、单一固定共享条款、撤回经治理角色处置、不做用户反馈；用户指令「按 next_action 执行 speckit-specify」。

## 一句话说明

已登录的 cogseed 客户端用户能把自己创建并验证过的本机 Skill 贡献到 Hub：客户端先做本地检查，再让贡献者预览将要发出的全部内容并完成来源声明、条款接受与维护责任确认后提交；贡献者在客户端「我的贡献」中看到提交与审核状态、提交新版本、申请撤回。审核、发布、目录、安装、更新、停用全部在 Hub 侧与迭代一链路完成，本文不重复其规则。只做 Skill。

## 与 Hub 侧规格的分工

Hub 侧规格定义了整条链路（用户故事 1～4、FR-001～FR-027、SC-001～SC-008）。本文只承接**客户端必须实现**的部分：

| Hub 侧 | 客户端承接 |
|---|---|
| 用户故事 1（提交）| 本文用户故事 1、2 |
| 用户故事 2（运营审核发布）| 不承接；客户端只展示退回原因（用户故事 3）与目录来源标识（沿用迭代一 G-B）|
| 用户故事 3（修订维护）| 本文用户故事 3、4 |
| 用户故事 4（撤回）| 本文用户故事 5 |
| FR-001、FR-003～FR-008、FR-011、FR-012、FR-018、FR-019、FR-022～FR-025 | 本文 C-FR 对应条目逐条映射 |
| FR-002、FR-002a、FR-009、FR-010、FR-013～FR-017、FR-020、FR-026、FR-027 | 服务端职责；客户端只展示服务端返回的拒绝原因 |

## 用户场景与测试

### 用户故事 1：登录用户对本机 Skill 发起贡献并通过本地检查（优先级：P1）

已登录用户在客户端「自定义 Skill 列表」对一个本机 Skill 选择「贡献到 Hub」。客户端在任何内容离开本机之前，按迭代一 §6.3 执行包规则、结构质量门与禁止内容检查。任一阻断项存在时提交流程停在本机，错误定位到具体文件或规则；贡献者修改后可重试。

**优先级原因**：本地检查是「内容不得离开本机」这一隐私承诺的唯一守门人，也是所有后续步骤的前提。

**独立测试**：准备三个本机 Skill——一个合规、一个含本机绝对路径、一个超过 200 个文件——分别选择「贡献到 Hub」；验证前者进入预览，后两者被阻断并定位到文件/规则，且网络层无任何上传请求。用未登录状态验证入口不可见。

**验收场景**：

1. **假如**用户已登录且本机存在自定义 Skill，**当**打开自定义 Skill 列表，**那么**每个本机 Skill 提供「贡献到 Hub」入口。
2. **假如**用户未登录，**当**打开自定义 Skill 列表，**那么**不提供「贡献到 Hub」入口；平台内置 Skill、Hub 安装的官方副本不提供该入口。
3. **假如**用户选择「贡献到 Hub」，**当**客户端执行本地检查，**那么**检查覆盖迭代一 §6.3 全部规则：`SKILL.md` 入口与 `name`、安全边界（路径穿越、绝对路径、重复/大小写冲突路径、符号链接、异常压缩比）、上限（压缩后 ≤ 8 MiB、≤ 200 个文件、解包 ≤ 10 MiB）、禁止内容（真实凭据、用户任务正文、Prompt、私人实例 ID、本机绝对路径）、结构质量门 `EXTREME` 级。
4. **假如**本地检查发现任一阻断项，**当**检查完成，**那么**客户端展示每个阻断项对应的文件路径或规则名，不进入预览，不发出任何网络请求，贡献者可修改后重试。
5. **假如**本地检查只发现 `MEDIUM` / `LOW` 级提示项，**当**检查完成，**那么**提示项随预览一并展示，不阻断。
6. **假如**本地检查通过，**当**检查完成，**那么**客户端进入提交预览（用户故事 2）。
7. **假如**Hub 侧尚未标记生效的条款版本（C-FR-013），**当**用户选择「贡献到 Hub」，**那么**客户端说明当前未开放贡献，不进入检查。

---

### 用户故事 2：贡献者预览、完成三项声明并提交（优先级：P1）

本地检查通过后，客户端展示提交预览：完整文件清单、每个文件的只读内容、将随提交发出的元信息（名称、说明、版本、来源声明、条款版本、贡献者显示名）。贡献者填写来源声明、勾选接受《CogSeed 社区贡献条款》、确认维护责任后提交。提交成功后贡献者在「我的贡献」看到该提交为「待审核」。

**优先级原因**：这是迭代二唯一新建的机制（客户端 → Hub 的上传方向）；没有它社区贡献不存在。

**独立测试**：用登录账号对合规 Skill 走完预览、声明、提交，对照 Hub 后台的待审核提交逐项核对文件清单与元信息；分别在缺来源声明、未勾选条款、未确认维护责任时点提交，验证被拒绝并提示缺项；对派生副本验证来源预填且关联不可删；在提交中断网后重试，验证 Hub 侧只有一条待审核提交。

**验收场景**：

1. **假如**本地检查通过，**当**进入预览，**那么**贡献者看到将发出的完整文件清单、可逐个打开的只读内容、以及元信息：名称、说明、版本、来源声明、条款版本、贡献者显示名。
2. **假如**贡献者在预览中，**当**查看任何文件，**那么**内容只读，不可在预览内编辑；需修改则退出预览、在本机修改后重新发起。
3. **假如**贡献者填写来源声明，**当**选择类型，**那么**至少可选「原创」「派生自 Hub 内容」「基于第三方材料」，并可填写补充说明。
4. **假如**被贡献的 Skill 是迭代一 §7.7 产生的派生副本（带 `forked_from`），**当**进入来源声明，**那么**类型固定为「派生自 Hub 内容」，预填「派生自 Hub 内容 {content_id} v{version}」；贡献者可补充说明但不能改类型、不能删除关联。
5. **假如**贡献者勾选接受条款，**当**勾选，**那么**客户端展示条款全文（或可打开的全文）及其版本号，提交时记录所接受的条款版本与勾选时间。
6. **假如**贡献者确认维护责任，**当**确认，**那么**客户端记录确认时间；确认文案说明该承诺不构成 Hub 对贡献者的强制义务。
7. **假如**来源声明、条款接受、维护责任确认三项任一缺失，**当**点提交，**那么**提交按钮不可用或提交被拒绝，并指出缺项。
8. **假如**三项齐全，**当**提交，**那么**客户端只发出预览中展示的文件与元信息；安装清单、用户设置、任务正文、设备信息、其他 Skill 均不随提交发出。
9. **假如**提交过程中网络中断，**当**恢复后贡献者重试，**那么**Hub 侧不产生重复的待审核提交；客户端明确告知本次提交是「已完整到达」还是「未到达，请重试」。
10. **假如**同一本机 Skill 已有一条待审核提交，**当**贡献者再次提交，**那么**客户端提示本次提交将替换原待审核提交，确认后替换而非并存。
11. **假如**服务端拒绝提交（未登录、待审核数量超上限、身份不符等），**当**收到拒绝，**那么**客户端展示服务端返回的原因；待审核超上限时提示等待审核完成。
12. **假如**提交成功，**当**回到「我的贡献」，**那么**能看到该提交，状态为「待审核」。

---

### 用户故事 3：贡献者查看「我的贡献」并撤回审核前的提交（优先级：P2）

贡献者在客户端「我的贡献」看到自己提交过的内容：稳定 ID、名称、当前发布版本、提交 / 审核 / 撤回状态；被退回时可看到退回原因；审核前可自行撤回提交，无需运营处置。

**优先级原因**：没有状态可见性，贡献者无从得知审核结果，也无法履行维护责任；审核前撤回是贡献者纠错的最低成本路径。

**独立测试**：在 Hub 后台分别把三条提交置为待审核、已退回（带原因）、已接受并发布，验证客户端列表状态与原因一致；对待审核提交执行撤回，验证 Hub 侧状态变为已撤回。

**验收场景**：

1. **假如**贡献者已登录，**当**打开「我的贡献」，**那么**看到自己提交过的内容列表，每项含稳定 ID（有则显示）、名称、当前发布版本（有则显示）、最近一次提交状态（待审核 / 已接受 / 已退回 / 已撤回）、撤回申请状态（有则显示）。
2. **假如**某条提交被退回，**当**查看该项，**那么**退回原因可见，并提供「修改后重新提交」的入口（走用户故事 1、2，作为新提交）。
3. **假如**某条提交处于待审核，**当**贡献者选择撤回提交并确认，**那么**该提交状态变为「已撤回」，不需运营处置。
4. **假如**列表数据来自上一次同步且当前网络不可用，**当**打开列表，**那么**展示上次同步结果并标注时间，恢复网络后自动刷新。
5. **假如**用户未登录或切换了账号，**当**打开「我的贡献」，**那么**只展示当前账号的数据，不展示其他账号的贡献记录。

---

### 用户故事 4：贡献者对已发布的社区内容提交新版本（优先级：P2）

贡献者修改本机对应的 Skill 后，从「我的贡献」对自己已发布（含已停用/已撤回）的社区内容选择「提交新版本」：关联原稳定 ID，版本号必须高于当前最高已发布版本，来源声明沿用上一版并可修改；其余走用户故事 1、2。

**优先级原因**：维护责任要靠新版本链路履行；问题版本没有新版本只能靠撤回。

**独立测试**：对一个已发布社区 Skill 提交新版本，验证提交携带原稳定 ID、版本号校验、来源声明预填；对已撤回的内容提交修复版本，验证允许提交并提示发布后仍需治理角色恢复。

**验收场景**：

1. **假如**贡献者在「我的贡献」对自己已发布的内容选择「提交新版本」，**当**进入提交，**那么**提交自动关联原稳定 ID，且客户端不允许修改该关联。
2. **假如**贡献者填写的版本号不高于当前最高已发布版本，**当**提交，**那么**客户端在本机阻断并说明当前最高已发布版本。
3. **假如**进入来源声明，**当**展示，**那么**预填上一版的来源声明，贡献者可修改（派生关联仍不可删）。
4. **假如**上一次新版本提交仍在待审核，**当**再次提交，**那么**按用户故事 2 场景 10 提示替换。
5. **假如**该内容处于停用或已撤回状态，**当**提交修复版本，**那么**允许提交，并提示：发布后仍需内容治理角色执行恢复，贡献者不能自行恢复。
6. **假如**本机已不存在与该稳定 ID 对应的 Skill，**当**选择「提交新版本」，**那么**客户端让贡献者从本机自定义 Skill 中选择一个作为新版本来源，并保留原稳定 ID 关联。

---

### 用户故事 5：贡献者申请撤回已发布的社区内容（优先级：P2）

贡献者在「我的贡献」对自己已发布的社区内容申请撤回并填写原因；撤回由内容治理角色处置后生效。客户端展示申请状态：待处置 / 已撤回 / 已拒绝（含拒绝原因）。不支持版本级撤回。

**优先级原因**：完成标准要求「撤回按规则生效」；贡献者不获得直接停用权，客户端必须把这一点表达清楚。

**独立测试**：申请撤回一条已发布内容，在 Hub 后台分别执行同意与拒绝，验证客户端状态与原因；尝试只撤回单个版本，验证被明确拒绝并建议提交修复版本。

**验收场景**：

1. **假如**贡献者对自己已发布的社区内容选择「申请撤回」，**当**填写原因并确认，**那么**客户端说明撤回需治理角色处置后生效，提交申请，状态显示「待处置」。
2. **假如**申请被治理角色同意，**当**客户端下次同步，**那么**该内容显示「已撤回」；已安装侧行为沿用迭代一 §6.7（官方副本阻断新使用、不删除、不动用户设置、派生副本不受影响、运行中使用不中止），本文不重复定义。
3. **假如**申请被治理角色拒绝，**当**客户端下次同步，**那么**显示「已拒绝」与拒绝原因，内容保持发布。
4. **假如**贡献者试图只撤回某一个版本，**当**操作，**那么**客户端不提供版本级撤回入口，并在内容级撤回入口旁说明「不支持撤回单个版本，建议提交修复版本」。
5. **假如**目标是未发布的提交或草稿，**当**申请撤回，**那么**按用户故事 3 场景 3 处理，不进入治理处置。
6. **假如**已存在待处置的撤回申请，**当**再次申请，**那么**客户端不重复提交，展示现有申请状态。

---

### 边界情况

- 贡献过程中账号登出或切换：进行中的检查/预览立即终止，不发出提交；「我的贡献」按新账号重新加载。
- 本地检查耗时（大包、深扫描）：显示可见进度，允许取消；取消后不留下任何临时上传产物。
- 预览中的文件为二进制或超长文本：以只读方式提示「二进制文件，{size}」或分段展示，不省略清单项。
- Skill 在预览期间被本机修改：提交前重新计算摘要，若与预览时不一致则要求重新检查与预览。
- 客户端版本低于 Hub 要求的最低版本：沿用迭代一「需要 CogSeed x+」的不兼容提示，不开放提交入口。
- 服务端检查结果比本地严格（提交进入待审核但附检查结果）：客户端展示服务端附带的检查结果，不视为失败。
- 来源声明为「派生自 Hub 内容」但上游内容已停用：允许提交，照记来源。
- 提交名称与本机其他 Skill 同名：沿用客户端既有规则；与 Hub 既有内容同名：客户端不预判，由 Hub 接受时阻断并通过退回原因回传。
- 「我的贡献」条目对应的本机 Skill 已被删除：条目保留，「提交新版本」走用户故事 4 场景 6。
- 条款改版：既有贡献不重签；新提交按当前生效版本展示并记录。

## 需求

### 功能需求

**入口与准入**

- **C-FR-001**（映射 FR-001）：客户端 MUST 在自定义 Skill 列表为每个本机自定义 Skill 提供「贡献到 Hub」入口；仅登录态可见；平台内置 Skill 与 Hub 安装的官方副本不提供该入口。
- **C-FR-002**（映射 FR-002）：客户端隐藏入口不替代服务端校验；客户端 MUST 把服务端对提交、新版本、撤回申请的拒绝原因原样展示给贡献者。
- **C-FR-002a**（映射 FR-002a）：待审核数量超上限被服务端拒绝时，客户端 MUST 提示等待审核完成。

**本地检查**

- **C-FR-003**（映射 FR-003）：客户端 MUST 在任何内容离开本机之前执行迭代一 §6.3 的全部规则：入口与元信息、安全边界、上限（压缩后 ≤ 8 MiB、≤ 200 个文件、解包 ≤ 10 MiB）、禁止内容（真实凭据、用户任务正文、Prompt、私人实例 ID、本机绝对路径）、结构质量门（`EXTREME` 阻断，`MEDIUM` / `LOW` 提示）。
- **C-FR-003a**：任一阻断项存在时，客户端 MUST NOT 发出任何网络请求；错误 MUST 定位到具体文件路径或规则名。
- **C-FR-003b**：本迭代 MUST NOT 做自动脱敏或自动改写；禁止内容由贡献者自行修改后重试。

**提交预览**

- **C-FR-004**（映射 FR-004）：客户端 MUST 在提交前展示完整文件清单、每个文件的只读内容，以及将发出的元信息：名称、说明、版本、来源声明、条款版本、贡献者显示名。
- **C-FR-004a**：预览之外的任何本机数据（安装清单、用户设置、任务正文、设备信息、其他 Skill、账号凭据）MUST NOT 随提交发出；实际发出的文件清单 MUST 与预览逐项一致。

**三项声明**

- **C-FR-005**（映射 FR-005）：来源声明、条款接受、维护责任确认三项缺一，客户端 MUST 拒绝提交并指出缺项。
- **C-FR-006**（映射 FR-006）：来源声明 MUST 至少支持「原创」「派生自 Hub 内容」「基于第三方材料」并允许补充说明；带 `forked_from` 的派生副本 MUST 自动预填「派生自 Hub 内容 {content_id} v{version}」，类型与关联不可修改或删除。
- **C-FR-007**（映射 FR-007）：客户端 MUST 展示当前生效的《CogSeed 社区贡献条款》全文与版本号，贡献者勾选接受时记录条款版本与时间；条款改版后既有贡献不重签。
- **C-FR-008**（映射 FR-008）：维护责任确认 MUST 记录确认时间；文案说明该承诺不构成 Hub 对贡献者的强制义务。

**提交行为**

- **C-FR-011**（映射 FR-011）：同一本机 Skill 审核前重复提交时，客户端 MUST 提示将替换原待审核提交，确认后替换。
- **C-FR-012**（映射 FR-012）：提交 MUST 幂等：网络中断后重试不产生重复待审核提交；客户端 MUST 明确告知「已完整到达」或「未到达，请重试」，不出现中间态。
- **C-FR-012a**：提交前 MUST 重新校验包摘要；与预览时不一致则要求重新检查与预览。

**我的贡献**

- **C-FR-018**（映射 FR-018）：客户端 MUST 提供「我的贡献」列表：稳定 ID、名称、当前发布版本、最近提交状态（待审核 / 已接受 / 已退回 / 已撤回）、撤回申请状态（待处置 / 已撤回 / 已拒绝）、退回与拒绝原因。
- **C-FR-018a**：列表只展示当前登录账号的数据；账号切换时 MUST 重新加载，不得展示其他账号数据。
- **C-FR-018b**：列表 MUST 支持离线展示上次同步结果并标注同步时间，网络恢复后自动刷新。
- **C-FR-025**（映射 FR-025）：待审核提交 MUST 可由贡献者自行撤回，状态置为「已撤回」，不需运营处置。

**提交新版本**

- **C-FR-019**（映射 FR-019）：客户端 MUST 支持对自己已发布（含已停用 / 已撤回）的社区内容提交新版本：自动关联原稳定 ID 且不可修改；版本号必须高于当前最高已发布版本（客户端本机阻断）；来源声明预填上一版并可修改；本地检查、预览、三项声明按 C-FR-003～C-FR-008 执行。
- **C-FR-019a**：对停用 / 已撤回内容提交修复版本时，客户端 MUST 提示发布后仍需内容治理角色执行恢复。
- **C-FR-019b**：本机已无对应 Skill 时，MUST 允许贡献者选择一个本机自定义 Skill 作为新版本来源，并保留稳定 ID 关联。

**撤回申请**

- **C-FR-022**（映射 FR-022）：客户端 MUST 支持对自己已发布的社区内容申请撤回并填写原因；展示状态待处置 / 已撤回 / 已拒绝（含拒绝原因）。
- **C-FR-023**（映射 FR-023）：客户端 MUST 在申请前说明撤回经治理角色处置后生效，贡献者无直接停用权。
- **C-FR-024**（映射 FR-024）：客户端 MUST NOT 提供版本级撤回入口，并在内容级撤回处说明不支持并建议提交修复版本。
- **C-FR-024a**：已有待处置申请时 MUST NOT 重复提交申请。

**已安装侧（沿用迭代一，不新增需求）**

- **C-FR-030**：社区内容撤回后的已安装侧行为（阻断新使用、不删除、不动用户设置、派生副本不受影响、运行中使用不中止）与修订版本发布后的自动更新，MUST 沿用迭代一 §6.7、§7.5 的客户端实现；本文不重复定义，也不因本迭代新增分支。
- **C-FR-031**（映射 FR-016）：目录中社区内容的「社区贡献 + 贡献者显示名」来源标识属于迭代一 G-B 客户端目录改造，本文只要求不与其冲突。

**跨切面**

- **C-FR-040**：所有新增可见文案 MUST 进入 renderer/main locales 并支持中英文；「贡献到 Hub」「我的贡献」等命名沿用本文用语。
- **C-FR-041**：本地检查、上传、列表刷新等耗时操作 MUST 提供可见进度；上传 MUST 可取消。
- **C-FR-042**：日志与遥测 MUST NOT 记录包正文、文件内容、条款全文或贡献者身份以外的账号信息；只记录提交 ID、稳定 ID、状态、计数、耗时与粗粒度错误类型。

### 关键实体

- **本机自定义 Skill**：用户在客户端创建或导入、位于自定义 Skill 列表的 Skill；可能带 `forked_from`（派生副本）。是贡献的唯一来源类型。
- **本地检查结果**：对一个本机 Skill 执行 §6.3 规则后的产物：阻断项列表（文件路径或规则名）、提示项列表、规范化文件清单、包摘要。只存在于本机。
- **提交预览**：将要发出内容的只读快照：文件清单、每个文件内容、元信息（名称、说明、版本、来源声明、条款版本、贡献者显示名）、包摘要。
- **三项声明**：来源声明（类型 + 说明 + 可选派生关联 {content_id, version}）、条款接受（条款版本 + 时间）、维护责任确认（时间）。
- **提交（客户端视角）**：一次贡献动作在客户端保留的记录：提交 ID（Hub 分配）、关联本机 Skill、关联稳定 ID（可空）、版本、状态、退回原因、提交时间。用于「我的贡献」展示与幂等重试。
- **我的贡献条目**：一个稳定 ID 或一条未获稳定 ID 的提交在客户端的聚合视图：名称、当前发布版本、最近提交状态、撤回申请状态与原因。
- **撤回申请（客户端视角）**：稳定 ID、原因、状态（待处置 / 已撤回 / 已拒绝）、处置说明。
- **社区贡献条款**：Hub 提供的单一条款文本及其版本号与生效时间；客户端只读取与展示。

## 成功标准

### 可衡量结果

- **C-SC-001**（承接 SC-003）：含 §6.3 禁止内容或超限的包 100% 在本地检查阶段被阻断，网络层零请求，每个阻断项定位到文件或规则；macOS 与 Windows 各验一遍。
- **C-SC-002**（承接 SC-004）：贡献者在预览中看到的文件清单与 Hub 侧收到的文件清单逐项一致，预览外无任何本机数据发出；用抓包对照验证。
- **C-SC-003**（承接 SC-001 客户端部分）：一名登录用户对真实 Skill 从选择入口到提交成功，在 5 分钟内可独立完成，无需查阅文档；三项声明缺项 100% 被拦截并指出缺项。
- **C-SC-004**（承接 FR-012）：在上传过程中人为断网并重试 10 次，Hub 侧待审核提交始终只有 1 条，客户端 10 次均给出明确的「已到达 / 未到达」结论。
- **C-SC-005**（承接 FR-018）：Hub 侧状态变更（退回、接受、撤回处置）在客户端下一次刷新周期内可见，退回与拒绝原因与后台一致。
- **C-SC-006**（承接 FR-019 / FR-024）：非贡献者账号在客户端看不到他人内容的「提交新版本」「申请撤回」入口；版本号不高于当前最高已发布版本的新版本提交 100% 在本机被阻断；不存在任何版本级撤回入口。
- **C-SC-007**：日志与遥测抽样 100% 不含包正文、文件内容、条款全文。

## 假设

- **迭代一客户端切片是前置依赖**：本文所有「沿用迭代一」处依赖 cogseed-hub PRD doc-v0.5 的客户端实现（Hub 目录协议 A-01/A-02、`forked_from` 派生副本 §7.7、内容级停用 §6.7、自动更新 §7.5、来源标识）。截至 2026-09-18，本仓库尚无这些能力的规格与实现（`EV-HUB-OPEN1-CLIENT-BASELINE-001` 记录了 5 项协议差异，`src/main` 中无 `forked_from`）；迭代一 G-A / G-B 未关闭。因此 C-FR-006 的派生预填、C-FR-030、C-FR-031 以及 Hub 侧 SC-001 / SC-005 / SC-006 在迭代一客户端切片交付前不能验收。Plan 阶段需决定先补迭代一客户端切片还是并行。
- **上传方向契约在 Plan 阶段冻结**：提交 / 新版本 / 撤回申请的接口契约与幂等键、本地检查结果的结构、条款版本与全文的获取方式、「我的贡献」状态同步方式，Hub 侧规格未定义，由客户端 RD 与 Hub RD 在 Plan 的 contracts/ 中冻结并以合同测试覆盖。本文只约束行为，不约束契约形态。
- **条款后台开关是运行时闸门**：Hub 侧未配置并标记生效条款版本前，客户端不开放贡献入口（用户故事 1 场景 7）；「是否开放」的判定依据在 Plan 阶段与条款获取方式一并冻结。
- **本地检查复用既有能力**：客户端已有的导入检查（8 MiB / 200 文件 / 10 MiB 上限、`EXTREME` 阻断级、深度扫描、安全阈值社区档）作为 C-FR-003 的基础；禁止内容清单中「用户任务正文」「Prompt」「私人实例 ID」的识别规则若既有扫描未覆盖，Plan 阶段补充并记录识别边界。不做自动脱敏。
- **贡献者显示名**：取账号服务已有的社区资料显示名；显示名为空时不能提交并引导用户先设置（本迭代不在客户端提供设置入口，与 Hub 侧「不提供匿名贡献」一致）。
- **提交幂等**：由客户端为每次贡献动作生成一个稳定的幂等键并在重试中沿用；键的形态在 Plan 冻结。
- **「我的贡献」数据以 Hub 为准**：客户端本地只缓存上次同步结果供离线展示，不做本地权威状态。
- **客户端改动边界**：按 Handoff 假设为「复用 Marketplace 定点改造」；若 Plan 发现需要超出该边界（例如新建独立数据域或新增 npm 依赖），回 Owner 决定。
- **不做**（照 COGSEED-313.1 评论第 2 节，勿在客户端扩）：用户反馈、评论评分、其他内容类型（Agent / 模板）、自动脱敏、匿名贡献、贡献者协作维护、激励排行、审核 SLA、批量审核、版本级撤回、派生副本与上游差异比较、受邀机制 / 邀请码。提交对象按通用内容对象设计，但本迭代不为 Agent / 模板预留表单或校验分支。

## AI Product Decision Contract

### Evidence baseline

- Evidence IDs and source types：`EV-HUB-OPEN2-ROUTE-001`、`EV-HUB-OPEN2-QUALIFY-001`（Hub 侧路由与资格判定，real）；`EV-HUB-OPEN1-CLIENT-BASELINE-001`（客户端 Marketplace 现状跨仓静态核查，E1 real）；MeshSeed `COGSEED-313.1` 正文与评论 #89（self-report）。
- Facts and direct quotes：Owner 2026-09-18 三项决定与范围决定；Hub 侧 Handoff `accepted_at: 2026-09-18T14:17:47+08:00`，`blocking_gaps: []`；客户端 `src/main` 无 `forked_from`；账号服务类型已含 `community_profile.display_name`。
- Stakeholder views and hypotheses：客户端改动可在「复用 Marketplace 定点改造」内完成（Handoff open assumption）；贡献者能在预览中自查敏感内容（Hub 侧假设）。
- What current evidence does **not** prove：客户端既有扫描能识别「用户任务正文 / Prompt / 私人实例 ID」；上传方向契约可行；迭代一客户端切片的交付时间。
- Current status：Submitted（本文为客户端侧 Draft，未经评审；Hub 侧契约为 Accepted）。

### AI suitability and authority

- Non-AI/Rules/Retrieval/Generation/Agent alternatives considered：本地检查与禁止内容识别采用规则 / 模式匹配；不引入模型判定。
- Why AI is or is not necessary：本迭代无 AI 决策；贡献链路是确定性规则与人工审核。
- Failure cost, visibility and reversibility：最高风险是敏感内容离开本机（不可逆）；由 C-FR-003a / C-FR-004a 与预览自查双重防护；提交后审核前可撤回。
- Allowed and prohibited data：允许发出预览中的 Skill 文件与元信息；禁止发出安装清单、用户设置、任务正文、设备信息、其他 Skill、账号凭据。
- Minimum authority level A0–A4：A2（沿用 `AUTH-HUB-OPEN2-CONTRIBUTION-001`）；A4 disabled。
- Human confirmation, audit and rollback points：提交前三项声明与预览确认；替换待审核提交需确认；撤回申请需确认；审计在 Hub 侧。

### Product states and protection surfaces

- Success, failure, refusal and empty states：成功 = 提交到达并显示待审核；失败 = 本地阻断（定位到文件/规则）或未到达（提示重试）；拒绝 = 服务端原因原样展示；空 = 「我的贡献」无记录、未登录无入口、条款未生效不开放。
- Must-not-happen outcomes：阻断项存在时发出网络请求；预览外数据随提交发出；重试产生重复待审核提交；展示其他账号的贡献记录；提供版本级撤回入口。
- Evaluation dataset, threshold and stop condition：本地检查用固定样例集（合规 / 各类阻断项 / 提示项）逐条验证 100% 阻断；抓包对照预览清单；断网重试 10 次幂等。
- Owner decisions still required：无新增；「先补迭代一客户端切片还是并行」为 Plan 阶段研发决定，超出 Marketplace 定点改造时回 Owner。

### Discovery handoff

- Primary uncertainty：`clear`（客户端侧路由 2026-09-18；集成契约由 Handoff 划归 Plan）。
- Validation vehicle or reason for direct-to-spec：direct-to-spec；Hub 侧 Discovery Gate approved、Spec Handoff Gate passed、Handoff accepted。
- Discovery Gate reference and status：`GATE-DISCOVERY-HUB-OPEN2-001` approved；`GATE-SPEC-HANDOFF-HUB-OPEN2-001` passed（均为 Hub 侧记录；客户端侧无独立 Gate）。
- Open assumptions retained in the specification：见「假设」全部条目；下一人工 Gate 为 MeshSeed `COGSEED-313.2` 需求 PRD 评审（未开始）。
